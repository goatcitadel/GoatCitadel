Brand sources:
- goatcitadel-mark.svg       full-detail mark (app icon, favicon.svg, tiles, >= 32px)
- goatcitadel-mark-tray.svg  heavier mark for <= 24px (tray, 16/24 favicon + ico frames)
- goatcitadel-logo-source.png raster lockup (README / wordmark / lockup only)

Edit the SVGs, then run: pnpm brand:derive
That regenerates web brand assets, Tauri icons, and WinUI Assets/.
