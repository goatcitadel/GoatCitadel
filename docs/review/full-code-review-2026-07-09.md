# GoatCitadel Full Code Review — 2026-07-09

## Review Contract

- Review base: `c28b54bf1276482067f36bcfd12429ceca1f76c4` (`origin/main` at review start)
- Serial delivery:
  - `codex/review-00-proof-integrity` merged as PR #205 at `df0b3576ac1c4ae32efb44f6d94593b0886c7949`
  - `codex/review-01-network-side-effects` merged as PR #206 at `4df18536833a453c0e31f3a164c8d988abb14bc4`
  - `codex/review-02-secret-projection-containment` merged as PR #207 at `a183833af23ffb73cb48233736ff913d4796eb6e`
  - `codex/review-03-approval-commit-truth` is the current approval/data-integrity branch (PR #208)
  - Review-04 and the final publication-SHA adversarial/proof pass remain pending after Review-03
- Canonical product surface: one Chat surface backed by the Gateway runtime
- Finding priorities: correctness, security, data durability, orchestration/agentic behavior, performance, runtime truth, and operator-facing UX behavior
- Excluded local state: `.env`, runtime databases, transcripts, audit logs, backups, dependency trees, build output, generated verification artifacts, and ignored legacy Mission Control residue
- Generated assets: review generator, provenance, schema, packaging, and regression behavior rather than generated rows or pixels line by line

## Finding Record

Every material finding records severity, confidence, release tier, certainty, evidence, reproduction, root cause, remediation, focused proof, broad proof, and final status.

Severity is `critical`, `high`, `medium`, or `low`. Release tier is `shipped`, `compatibility`, `experimental`, `generated`, `test-harness`, or `release-ops`. Certainty is `confirmed`, `likely`, or `runtime-validation-needed`.

## Baseline

| Check | Result | Evidence |
| --- | --- | --- |
| Git parity | Pass | Review base matched `origin/main`; clean dedicated worktree |
| Lint | Pass | `pnpm lint --max-warnings 0` |
| Typecheck | Pass | `pnpm typecheck` across 14 workspace projects |
| Build | Pass | `pnpm build`; canonical web and desktop bundles built |
| Fresh coverage | Blocked on canonical Linux evidence | The earlier Windows collection reported 67.30% lines, 79.23% branches, and 89.62% functions, but the hosted Linux run on pushed Review-03 source exposed platform-dependent inflation. Production gating now rejects non-Linux collector artifacts. |
| Production coverage tiers | Blocked pending final location-aware Linux evidence | The Review-03 hosted 88.63% storage result and later dirty-source 88.64% result both predate FR-274's location-aware cross-collector merger and are not valid final percentages. The gate-owned 90% storage-lines and 70% policy/security-branches floors remain unchanged; no threshold or exclusion is being relaxed. |
| Documentation governance | Pass | `pnpm docs:check` |
| Repo/supply-chain posture | Pass | `pnpm verify:repo:hygiene`; `pnpm verify:supply-chain` |
| Agentic contracts/governance | Pass on review base | Fresh main baseline captured before this branch |
| Architecture metrics | Pass on clean local source; hosted exact-SHA proof pending | Source commit `f9d3e238fb646ca36b93021194fb6f7cfc9eb3bc` captured the corrected metric maps only after every measured path was clean; baseline commit `81412ff964fee1b122c40a39d8955ef5ed3dd8ef` records that provenance. Exact-baseline artifact `2026-07-12T00-46-02-800Z-architecture-metrics-c7ad8265` passed: GatewayService is 8,441 lines, total host callbacks 913, Chat callbacks 276, and dependency-member accesses 6,738. |
| Orchestration performance | Initial misleading pass; fix in progress | Original CLI reported three fixed samples dated 2026-06-17 instead of measuring current runtime |
| GitHub CodeQL | Pass | No open code-scanning alerts; current base SHA CodeQL runs succeeded |
| GitHub Dependabot | Pass | No open Dependabot alerts |
| GitHub secret scanning | Fixed | Secret scanning and push protection enabled; zero alerts immediately after enablement |

## Test Runner and Named/CI Lane Map

This map is derived from the current root/package scripts, `scripts/verification/run.mjs`, its scenario owners, and every checked-in workflow. A runner invocation is not automatically release proof: the named lane owns the manifest/artifacts, and the hosted workflow column states where that lane is actually exercised.

| Runner or executor | Direct owner commands | Named verification coverage | Hosted CI/release owner |
| --- | --- | --- | --- |
| Vitest | Most TypeScript workspaces expose `test = vitest run` and `test:coverage = vitest run --coverage...`; Gateway's `test` script runs Vitest before two `tsx --test` files. `pnpm test` recursively invokes workspace owners. | `pnpm verify:fast` explicitly runs Gateway, storage, Mission Control Next, policy-engine, library, and desktop package tests. `pnpm coverage:collect` invokes package coverage owners and is gated by `coverage:gate:production`. | `verification-fast.yml` runs `verify:fast` and final Linux coverage. `publish-contracts.yml` also runs the contracts package test before publish. |
| Node test runner through `tsx` | Storage owns `tsx --test "src/**/*.test.ts"`; Gateway appends `tsx --test` for generated-artifact and thread-knowledge tests; migration integrity uses `pnpm --filter @goatcitadel/storage exec tsx --test ...`. | Storage and Gateway `tsx` tests are included by `verify:fast`; `verify:storage:migration-parity` owns the dedicated migration-integrity invocation. | `verification-fast.yml` is the hosted owner through `verify:fast` and the explicit migration-parity scenario inside it. |
| Node native `--test` and verification modules | Root aliases include `verify:repo:hygiene`, `verify:supply-chain`, `verify:artifacts:redaction`, `verify:mcp:conformance`, and `verify:storage:migration-parity`; `docs:check` also invokes checked Node governance modules. | Repo hygiene, migration parity, and docs are scenarios inside `verify:fast`; the remaining aliases are explicit named proof commands. `.mjs` verification/packaging/release code is linted and its checked-in `*.test.mjs` suites run through repo hygiene. | `verification-fast.yml` runs workflow validation, docs, fast proof, artifact redaction, and final coverage; release-proof matrix jobs run artifact redaction after every named lane. |
| Real PostgreSQL 16 | `pnpm --filter @goatcitadel/storage test:postgres` builds storage and runs `src/postgres/real-postgres.test.ts` through `tsx --test`; it requires `GOATCITADEL_TEST_POSTGRES_URL`. | The real-database lane is intentionally separate from SQLite-backed `verify:fast` scenarios, while migration structure is covered by `verify:storage:migration-parity`. | `verification-fast.yml` provisions `postgres:16-alpine`, supplies the connection URL, and invokes `test:postgres` explicitly after `verify:fast`. |
| Playwright Chromium | Browser execution is embedded in the verification scenarios rather than exposed as a standalone package test. | `verify:ui:parity`, `verify:surface:regression`, and `verify:visual:regression` execute current UI/browser scenarios; `verify:visual:rebaseline` is the write-enabled maintenance lane. | `verification-1-0-release-proof.yml` installs Chromium and runs the three read-only lanes in its matrix. `visual-rebaseline.yml` is the manual Ubuntu-only baseline writer. `verification-fast.yml` installs Chromium but that installation alone is not browser proof. |
| C# / .NET 10 | `pnpm windows:test` invokes the WinUI test project. `verify:desktop:raw` independently runs the same project in Release configuration. | `pnpm verify:desktop` wraps `verify:desktop:raw` in the verification manifest and also owns native launcher proof. | The Windows matrix in `release-installers.yml` runs `windows:test` and `verify:desktop`; the desktop row in `verification-1-0-release-proof.yml` runs `verify:desktop` on `windows-latest`. |
| Rust / Cargo | `verify:desktop:raw` runs both `cargo check` and `cargo test` in `apps/mission-control-desktop/src-tauri`. Tauri package scripts run Cargo transitively during native builds. | `pnpm verify:desktop` is the named Rust/Tauri proof owner. | `verification-1-0-release-proof.yml` runs the desktop lane on Windows. `release-installers.yml` installs Rust and reruns the desktop lane before packaging Windows targets; macOS packaging installs Rust but is build/smoke evidence, not a standalone Cargo test lane. |
| Packaging and release scripts | Root aliases call checked-in Node modules: `package:bundle`, `package:desktop`, `package:windows-host`, `package:windows-msix`, `package:windows`, `package:macos`, `verify:install`, and the extension publish/package aliases. | `verify:desktop`, `verify:extensions:package`, install/package smoke scripts, repo hygiene, and release certificate checks divide proof by artifact owner. | `release-installers.yml` builds/smokes Windows, Linux, and macOS artifacts and assembles the commit-bound release; `publish-contracts.yml` and `publish-extensions-sdk.yml` own package publication gates. |
| GitHub Actions/workflow syntax | `pnpm verify:workflows` validates checked-in workflows and PowerShell syntax; hosted actions remain the authority for runner/platform behavior. | `verify:workflows` is a direct named check and is executed before `verify:fast` in hosted CI. | `verification-fast.yml` installs actionlint and runs the workflow check. The workflow inventory below records every hosted entrypoint so an unreferenced file is not mistaken for covered proof. |

### Checked-in Workflow Inventory

| Workflow | Concrete execution contract | Proof boundary |
| --- | --- | --- |
| `code-mode-sandbox-canary.yml` | Runs `verify:code-mode:sandbox` on Ubuntu and `verify:code-mode:hostile-sandbox` on Windows for pull requests/main pushes and uploads verification artifacts. | Dedicated Code Mode claim/canary gate; it does not establish hostile-code sandboxing beyond the named posture tests. |
| `code-quality.yml` | Runs zero-warning ESLint, workspace typecheck, and Node typography/contrast guards. | Static/type/UI guard only; no workspace runtime suite. |
| `publish-contracts.yml` | Builds and typechecks the workspace, tests contracts, runs `verify:api:compat`, then publishes on its release trigger. | Contracts package compatibility/publication gate. |
| `publish-extensions-sdk.yml` | Builds and typechecks, runs `verify:extensions:package`, then publishes on its release trigger. | Extensions SDK package/publication gate. |
| `release-installers.yml` | Platform matrices build and smoke Windows, Linux, and macOS bundles; Windows also runs .NET/native desktop proof. The release job verifies the artifact matrix, signs, waits for required proof workflows, and writes the release certificate. | Packaging/install/release authority; platform smoke is not a substitute for application runtime lanes. |
| `security-trivy.yml` | Runs the pinned Trivy filesystem action for vulnerability, secret, and misconfiguration scanning with High/Critical failure severity. | Hosted third-party filesystem scan; distinct from local `verify:supply-chain`. |
| `typescript-7-compiler.yml` | Runs `ts7:toolchain` and `ts7:benchmark` and uploads benchmark artifacts. | Compiler/toolchain experiment only. |
| `verification-1-0-release-proof.yml` | Runs the checked-in named release-proof matrix on the declared Ubuntu/Windows runner for each lane, then generates review output, redaction-checks it, and uploads artifacts. | Canonical scheduled/tag/manual named-lane proof matrix. |
| `verification-fast.yml` | Validates workflows, lints, installs Chromium, checks docs, runs `verify:fast`, exercises real PostgreSQL, collects/gates Linux coverage, and verifies artifact redaction. | Canonical pull-request/main fast/data/coverage gate. |
| `visual-rebaseline.yml` | Manually runs `verify:visual:rebaseline` on Ubuntu and opens a PR for changed PNG baselines. | Write-enabled maintenance workflow; generated pixels require human diff review. |

## Findings Ledger

### FR-001 — Orchestration performance lane does not execute or measure the runtime

- Severity: `high`
- Confidence: `high`
- Release tier: `test-harness`
- Certainty: `confirmed`
- Evidence: `apps/gateway/src/orchestration/performance-gates-cli.ts` supplies fixed timestamps, costs, counters, and passing quality gates.
- Reproduction: `pnpm verify:orchestration:perf` always reports `generatedAt=2026-06-17T00:00:03.000Z`, three samples, and the same sub-second latencies.
- Root cause: the CLI was implemented as a report-builder demonstration but named and documented as a performance verification lane; the report builder also accepted empty, invalid, backwards, negative, and non-finite sample data.
- Remediation: replace fixed samples with deterministic scenarios that execute current orchestration code and emit measured scenario plus aggregate artifacts; keep report arithmetic unit-tested.
- Focused proof: performance-gate unit tests and deterministic runner tests.
- Broad proof: `pnpm verify:orchestration:perf`, agentic proof, and `pnpm verify:fast`.
- Status: `partially fixed and honestly scoped — 11 measured routed engine runs, a production completion-service retry after injected 503, and zero duplicate dispatches; tool, approval pause/resume, durable/restart, long-context, concurrent-session, stalled transport, and variance-threshold scenarios remain open`

### FR-002 — Architecture baseline rejects the July decomposition without classifying its new seams

- Severity: `high`
- Confidence: `high`
- Release tier: `test-harness`
- Certainty: `confirmed`
- Evidence: the original verifier reported 950 raw `host.*` text matches against a 921 baseline while GatewayService shrank by 1,341 lines. The counter included comments, strings, `URL.host`, untyped lambda variables named `host`, and local backend variables.
- Reproduction: `pnpm verify:architecture:metrics` fails on the clean review base.
- Root cause: `architecture-metrics.mjs` used `/\bhost\./g` over raw source rather than parsed, symbol-bound TypeScript.
- Remediation: replace the regex with a TypeScript AST/symbol-aware counter, prove lexical lookalikes are ignored, audit the actual July delta, and pin the current typed-host baseline only after the ownership review.
- Focused proof: architecture metric unit/scenario tests.
- Broad proof: `pnpm verify:architecture:metrics`, typecheck, and `pnpm verify:fast`.
- Status: `fixed, with later collector hardening tracked by FR-267 — the AST/symbol-aware owner removed raw-text false positives; FR-267 subsequently found alias/element/destructuring false negatives, recomputed published source a20384e6 at 917 rather than 914, and requires final-source architecture proof`

### FR-003 — Gateway/shared coverage floor lagged its declared next ratchet

- Severity: `medium`
- Confidence: `high`
- Release tier: `test-harness`
- Certainty: `confirmed`
- Evidence: the historical pre-fix collector reported 60.60% Gateway/shared lines while the enforced floor remained 52%. A later Review-03 publication artifact reported 59.98% against the corrected 58% floor, but FR-274 proved both percentages used a cross-report merger that treated collector-local Istanbul numeric IDs as stable identities. Those measurements are historical and are not valid final coverage evidence.
- Reproduction: the original `pnpm coverage:collect && pnpm coverage:gate:production` passed with the stale 52% tier threshold; swapping numeric IDs between equivalent Vitest/V8 and c8 maps also changes the old merger's attribution without changing source locations.
- Root cause: the coverage policy and comments were not updated after coverage recovered above the documented next target.
- Remediation: enforce 58% as the stability floor, retain 60% as the next ratchet, add regression coverage around current orchestration blind spots, and require a location-aware final Linux measurement before claiming the floor is met.
- Focused proof: coverage tooling tests plus a final location-aware Linux collection.
- Broad proof: `pnpm coverage:gate:production` and Verification Fast CI.
- Status: `policy fix implemented; percentage proof invalidated pending final Linux collection — the 58%/70% gate-owned floor remains, but the earlier 60.60% and 59.98% figures predate FR-274's location-aware merger and are non-authoritative`

### FR-004 — GitHub secret scanning and push protection are disabled on a public repository

- Severity: `medium`
- Confidence: `high`
- Release tier: `release-ops`
- Certainty: `confirmed`
- Evidence: GitHub repository API reports `secret_scanning=disabled` and `secret_scanning_push_protection=disabled`; the repository is public.
- Reproduction: query `repos/goatcitadel/GoatCitadel` security analysis state and the secret-scanning alerts endpoint.
- Root cause: repository security settings are not enabled despite the checked-in narrow fixture allowlist and synthetic-token guidance.
- Remediation: verify fixture exclusions, enable secret scanning and push protection, then query alerts and triage any result under `docs/security/findings-triage.md`.
- Focused proof: GitHub security settings and alerts API queries.
- Broad proof: CodeQL, Trivy, artifact redaction, supply-chain posture, and repository hygiene.
- Status: `fixed and verified — secret scanning and push protection enabled; alert API returned zero alerts`

### FR-005 — Two release-proof matrix lanes cannot produce the artifacts their workflow requires

- Severity: `high`
- Confidence: `high`
- Release tier: `release-ops`
- Certainty: `confirmed`
- Evidence: `verify:desktop` and `verify:extensions:package` originally ran standalone scripts, while the release workflow always followed each lane with `verify:review` and uploaded `artifacts/verification` using fail-on-missing behavior.
- Reproduction: hosted release run `29011596084` completed both underlying commands, then failed review/artifact steps because no verification run existed.
- Root cause: the matrix contract assumes every lane uses `scripts/verification/run.mjs`, but these two aliases bypassed it.
- Remediation: wrap both commands in named verification-context lanes, preserve raw internal aliases, and statically require every matrix lane to be verification-backed.
- Focused proof: release-certificate tests and the extensions package lane.
- Broad proof: scheduled release workflow on the publication SHA, including Windows desktop.
- Status: `fixed on branch — hosted Windows confirmation pending`

### FR-006 — Coverage gating accepts stale successful artifacts

- Severity: `medium`
- Confidence: `high`
- Release tier: `test-harness`
- Certainty: `confirmed`
- Evidence: `coverage-gate.mjs` originally validated percentages and status but carried no source/test revision or content fingerprint.
- Reproduction: copy any prior successful `coverage-summary.json` over changed source and run `pnpm coverage:gate`; the original gate passed it.
- Root cause: collection provenance ended at a random run ID and timestamps that were never compared with current source.
- Remediation: hash current apps, packages, scripts, and coverage-affecting root configuration; record that fingerprint during collection and fail gating when it differs.
- Focused proof: coverage-tooling stale-artifact regression.
- Broad proof: fresh collection followed by production gating.
- Status: `fixed and proven — apps/packages/scripts plus coverage-affecting root configs are hashed; Review-03 publication-source stored and recomputed fingerprint sha256:08f05893... matched; production gate passed`

### FR-007 — Verification, packaging, and release modules were excluded from ESLint

- Severity: `medium`
- Confidence: `high`
- Release tier: `test-harness/release-ops`
- Certainty: `confirmed`
- Evidence: the root ESLint config globally ignored every `.mjs` and `.cjs` file, covering 125 Node modules that implement verification, release, packaging, coverage, and CLI behavior.
- Reproduction: `pnpm lint` skipped those files; explicit no-ignore lint found an uncaught-error provenance defect in `benchmark/lib/targets.mjs` plus legacy script-rule incompatibilities.
- Root cause: Node and browser-injected globals were never modeled for script modules, so they were excluded wholesale.
- Remediation: add an explicit script-module ESLint environment/rule profile, remove global ignores, fix the confirmed error-cause defect, and discover all script tests rather than maintaining a partial hand list.
- Focused proof: release proof static regression, all script tests, and zero-warning lint.
- Broad proof: `verify:repo:hygiene` and Code Quality CI.
- Status: `fixed and proven — zero-warning full lint and all 115 discovered script tests passed`

### FR-008 — Cold contracts coverage import can exceed the generic unit timeout

- Severity: `low`
- Confidence: `high`
- Release tier: `test-harness`
- Certainty: `confirmed`
- Evidence: the first post-change `coverage:collect` failed only because `packages/contracts/src/module-load-smoke.test.ts` reached the generic 5-second timeout during a cold V8 coverage transform; the immediate focused rerun passed all 317 tests.
- Reproduction: run workspace coverage from cold build/transform state on Windows; the full contracts barrel import can cross five seconds.
- Root cause: a full export-barrel transform used a latency ceiling intended for ordinary unit tests.
- Remediation: give only that module-load smoke a documented 15-second ceiling; retain the global timeout everywhere else.
- Focused proof: contracts coverage suite.
- Broad proof: complete fresh coverage collection.
- Status: `fixed and proven — complete fresh collection passed all 317 contracts tests and the workspace`

### FR-009 — Failed orchestration performance gates discard their structured report

- Severity: `medium`
- Confidence: `high`
- Release tier: `test-harness/release-ops`
- Certainty: `confirmed`
- Evidence: the performance CLI writes its JSON artifact before setting a failing process exit code, while `runOrchestrationPerformanceLane` originally read that JSON only when the exit code was zero.
- Reproduction: make any performance threshold fail; the verification scenario records zero measured runs, no performance artifact, and raw logs instead of the report's threshold failures.
- Root cause: report availability was incorrectly coupled to process success even though the report is the canonical failure evidence.
- Remediation: read the report whenever it exists and use the exit code only as one input to pass/fail status.
- Focused proof: verification-scenario regression with a nonzero process result and a populated failing report.
- Broad proof: `pnpm verify:orchestration:perf`, repository hygiene, and the release-proof workflow.
- Status: `fixed and proven — the failing scenario retains measured metrics, threshold failures, and its performance artifact link`

## Confirmed Open Runtime Findings

The independent runtime, agentic, storage, provider, memory, integration, and native passes produced the following confirmed release blockers. These remain open after the proof-integrity branch and must be fixed in the serial branches named by the finding family.

### FR-101 — Cross-origin redirects forward credentials and mutation bodies

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: `packages/policy-engine/src/sandbox/network-guard.ts:183-243` and duplicate wrappers in `tool-executor.ts:683-720` and `comms-executor.ts:4043-4080` reuse the original request init on every redirect hop.
- Reproduction: an allowlisted `api.example.com` redirect to allowlisted `cdn.example.com` received the original `Authorization`, `Cookie`, `X-API-Key`, and POST body.
- Root cause: redirect destinations are network-validated, but request authority is not rebound to the new origin.
- Fix: centralize redirect authority policy and default-deny credential-bearing or body-carrying cross-origin redirects.
- Focused proof: two-origin policy-engine regressions; broad proof: security evals, channel/MCP/A2A tests, and fast lane.
- Status: `fixed, proven, and merged in PR #206 — one shared redirect-authority policy now blocks cross-origin credentials, bodies, and mutation methods before contact; same-origin authenticated and anonymous allowlisted GET redirects remain supported; central and both duplicate redirect loops are covered`

### FR-102 — Retry policy can duplicate ambiguous external writes

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: `tool-executor.ts` and `comms-executor.ts` retry 408/429/502/503/504 for every method, including Slack, Gmail, calendar, and webhook POST operations without transmitted provider idempotency keys.
- Reproduction: a Slack POST returning 503 and then 200 is issued twice and reported delivered once.
- Root cause: transport retryability is decided from status alone rather than HTTP method plus provider idempotency semantics.
- Fix: retry safe methods only by default; allow POST retry only when a stable provider-supported idempotency key is actually sent, otherwise enter manual reconciliation.
- Focused proof: ambiguous-success adapter tests; broad proof: external replay, channel runtime, and security eval lanes.
- Status: `fixed, proven, and merged in PR #206 — automatic retry is limited to GET/HEAD; ambiguous mutations issue once, become manual-reconciliation-required, retain that structured state through Gateway delivery, and cannot enter durable retry`

#### Review-01 closure evidence

- Canonical ownership: HTTP redirect/retry authority is centralized in the policy engine; channel-delivery persistence and compare-and-swap transitions remain storage-owned; Gateway and MCP layers project the canonical structured outcome without reclassifying an unknown external write as blocked, retryable, or successful.
- Redirect and generic HTTP proof: same-origin and cross-origin redirect matrices cover credentials, caller-derived headers, methods, bodies, allowlists, one-shot fetch behavior, response-body limits, and post-send failures. Automatic retries are limited to GET/HEAD; mutation responses and exceptions after dispatch carry `externalOutcome=unknown_after_send` and `manualReconciliationRequired=true`.
- Channel proof: Slack, Telegram, WhatsApp, Gmail, calendar, generic channel sends, rich/multipart sends, and bounded-body failures preserve manual-reconciliation state and any known provider message ID. SQLite delivery claims and terminal transitions are lease-fenced across overlapping drains, independent runtimes, delayed losers, restarts, callbacks, and stale snapshots; no proven pre-send failure is mislabeled as externally ambiguous.
- MCP proof: `tools/call` is replayed only for an explicit server result declaring `phase=pre_dispatch` and `retrySafe=true`. Stdio and remote-HTTP post-dispatch transport failures, JSON-RPC errors, and `isError` results issue once and retain structured unknown/manual truth through diagnostics, realtime evidence, approvals, and the public response.
- Dual-dialect proof: the nullable lease compare-and-swap predicate uses dialect-correct PostgreSQL typing. The live disposable PostgreSQL suite passed 2/2 tests, including a null-lease CAS transition and a stale-snapshot attempt that could not overwrite a sent row/provider ID; the SQLite storage suite passed 694 tests with 2 intentional skips.
- Owning suites: Gateway passed 5,291 Vitest tests plus 17 Node tests; policy engine passed 627 tests; contracts passed 317 tests; storage passed 694 tests with 2 skips. Contracts, storage, policy-engine, and Gateway typechecks passed, as did zero-warning lint over every changed source/test file.
- Named proof: `verify:api:compat` (`2026-07-09T22-50-28-663Z-api-compat-08cd53f1`), `verify:channels:runtime` (`2026-07-09T23-25-22-112Z-agentic-channels-runtime-77c0f039`), `verify:runtime:truth` (`2026-07-09T23-25-22-065Z-runtime-truth-a47c084a`), `verify:mcp:conformance` (6/6), and `verify:security:evals` (`2026-07-09T23-25-22-065Z-security-evals-f5c4ada1`) passed on the final source.
- Broad proof: `docs:check` passed; `verify:fast` passed as `2026-07-09T23-25-53-533Z-fast-9ebfbb57`. The architecture guard initially rejected 29 new facade lines; the logic was moved into the existing channel-delivery helper owner, leaving GatewayService at the verifier's authoritative 8,292 lines (43 below the 8,335 baseline), 900 typed host callbacks (zero delta), and a passing `2026-07-09T23-25-22-060Z-architecture-metrics-76332783` artifact without a baseline ratchet.
- Hosted merge proof: PR #206's final head `1bbf7e4504a856a5f9e02d07223c66e28321febf` passed hosted fast verification (including fresh production coverage, real PostgreSQL, and artifact-redaction gates), JavaScript/TypeScript, C#, and Actions CodeQL, lint, Trivy, and both Code Mode canaries. Every automated review thread was rechecked and confirmed addressed before squash merge `4df18536833a453c0e31f3a164c8d988abb14bc4`.
- Hosted-review closure: automated review found three adjacent boundary errors and each was reproduced with a failing regression before correction. Safe attachment/preflight GET responses could falsely mark a later pre-dispatch failure as an ambiguous channel mutation; request tracking now accumulates mutation-started and mutation-response boundaries separately. Explicit MCP `pre_dispatch` truth could be overridden by expired-session wording; that phase is now authoritative and is not projected as unknown/manual. An already-aborted `http.post` could reach fetch and become unknown; it now exits before dispatch, while a paired abort-after-fetch regression remains `unknown_after_send`. Attachment 404s, response-stream failures, and pre-dispatch policy blocks remain `not_available`/`blocked`, while successful upload followed by a later attachment failure remains manual. The boundary suites, the 25-test channel-failure suite, and the full owning suites passed.
- Independent closure: three adversarial passes and a post-review boundary re-review separately returned safe-to-merge verdicts after testing redirected mutation authority, delayed delivery losers, stale-runtime fencing, provider-ID retention, nullable PostgreSQL predicates, MCP replay, preflight-versus-mutation boundaries, and adjacent failure paths. Their only shared residual proof caveat was live PostgreSQL execution; the 2/2 live PostgreSQL run above closed it before publication.

### FR-103 — Inline secrets cross storage, API, approval, audit, and model boundaries

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: key-pattern-only scrubbing in `tool-security.ts`; raw approval/tool arguments and URLs in orchestration evidence; approval-explainer prompts; raw integration connection configs returned by generic APIs; A2A bearer tokens stored directly.
- Reproduction: a webhook URL containing `key`/`token`, a short bearer value, and `DATABASE_PASSWORD` produced no detections, survived sanitization, and could enter the explainer request.
- Root cause: executable configuration, persisted evidence, and public/model DTOs share raw objects; redaction is neither key-aware enough nor URL/string-aware.
- Fix: reject inline secrets where secret references are required, introduce sanitized public/evidence/model projections, migrate persisted credentials forward to secret references, and preserve only resolvable references in executable state.
- Focused proof: storage/API/explainer redaction regressions; broad proof: artifact redaction, auth matrix, migration parity, and channel/A2A lanes.
- Status: `in progress — review-02 delivered broad response/model/evidence containment; review-03 removes remote-approval plaintext bearers from durable state and scrubs historical rows, but the remaining forward secret-reference migration and secondary public-surface queue keep FR-103 open`

#### Review-02 containment scope and evidence

- Canonical ownership: contracts own context-aware structured/text redaction; the Gateway owns detached public/model projections and URL/argv/schema-specific shapes; storage/runtime owners retain raw executable state only where execution still requires it. Public projections never mutate service or repository objects.
- Response/model containment: approval REST/explainer/connector delivery, Chat threads/messages/streams/delegation/workbench execution, durable/orchestration/session/runtime exports, direct tool invocation, retained realtime, integrations/channels/hooks/comms, MCP, provider/settings/local-runtime status, agent/skill provenance, capability proposals/candidates/Code Mode, cron/Observe, A2A HTTP/JSON-RPC/gRPC/push, audit, evidence envelopes, and generated/tool artifacts now have regression-backed projection boundaries.
- Editable projections: integration/channel/hook settings, MCP argv/URLs/policy, llama.cpp command/args, provider request auth/headers/proxy/TLS/base URL, and cron config/output round trips restore only the current record's hidden leaves. Stable-ID arrays preserve per-record identity; ambiguous or moved markers fail closed; explicit empty arrays remain explicit. One-time auth, SSE, device/session, vault reveal, OAuth, remote-approval, and realtime-voice issuance responses remain intentionally unprojected.
- Truthful artifacts: newly stored evidence metadata is sanitized before hashing/writing. Legacy evidence and Code Mode/tool/generated-artifact reads are projected without rewriting the canonical bytes; additive `publicProjection` metadata states when content changed and that canonical hashes refer to stored artifacts.
- Realtime approval recovery: retained/list/default events contain no action token. Only an exact approval-resolution live event can carry the one-time browser token, and only to an operator-authenticated or direct loopback SSE subscriber; replay/live overlap prefers that authorized transient copy. Operator-only token reissue remains the reload/recovery path.
- Regression-first closure: every confirmed sink received a failing regression before its canonical projector/reconciliation fix. The first adversarial pass covered real image `b64Json`, settings GET-to-PATCH restoration, signed evidence manifests, schema-aware capability projection, context-manifest hash semantics, Chat metadata marker movement, and one- versus multi-tag reorder behavior. The final follow-up pass then reproduced and fixed cross-chunk secret reconstruction, stale-attempt writes/finalization, incomplete stream-codec variants, authorization scheme-plus-credential argv, duplicate-flag marker rebinding, cancellation unhandled rejection, and terminal-cancel compatibility during the 30-second retained-registration window. Hosted production coverage exposed one final proof/runtime-boundary regression: the token-aware realtime wrapper had bypassed EventEmitter's synchronous listener validation, allowing the reflective coverage sweep to register non-functions that crashed only during shutdown. Boundary validation and a publish-after-rejection regression now close that path.
- Stream projection closure: the canonical redactor passed 41 focused cases and the stateful stream projector passed eight exhaustive cases. An independent split-point harness exercised 4,598 boundaries across 135 credential/header/YAML/collection/Unicode/CLI/argv/raw-token cases with zero leaks or liveness failures; 1,201 cross-language safe-versus-secret code splits had zero safe-expression mutations or secret leaks. Canonical redaction stayed linear at 50k/100k/200k/400k inputs (42.6/84.1/163.2/328.4 ms), while the optimized projector processed 100,000 safe microchunks in about 220 ms and a 100,000-character unbroken safe chunk in about 5 ms on the review host.
- Runtime ownership and fidelity: `ChatTurnExecutionRegistry` now issues an immutable turn-and-registration-bound producer lease. Exact-lease write sequencing, completion, and close operations make last-registration-wins duplicate wakes fail closed without widening extracted-service hosts. Fresh retries remain live; pause/resume, user-input, retry, and lease-takeover continuations suppress unsafe partial text until a terminal chunk; stale producers cannot write errors, finalize durable state, or close a resumed stream. Stream codecs now round-trip `thinking_delta`, complete approval-governance fields, and `user_input_required` prompts.
- Editable projection closure: command/argv reconciliation covers `auth`, `authentication`, `authorization`, both proxy-authorization spellings, and `bearer` scheme pairs. It restores only the exact hidden raw slots, preserves safe sibling edits, and rejects duplicate credential-flag removal/reordering instead of rebinding a marker to a different secret.
- Owning suites on the final source: contracts 355/355; policy engine 630/630; storage 695 passed with 2 intentional skips; Gateway 5,454/5,454 Vitest tests plus 17/17 Node tests (5,471 total). Contracts, policy-engine, storage, and Gateway typechecks passed, as did strict zero-warning lint over every changed TypeScript file, formatting, and `git diff --check`.
- Security/data proof: artifact redaction passed 5/5 after every artifact-producing lane; auth matrix `2026-07-10T14-16-41-774Z-auth-matrix-7a1a3ce0`; security evals `2026-07-10T14-16-58-565Z-security-evals-57e99940`. The final fast lane refreshed repository hygiene at 118/118, including the six supply-chain posture cases.
- Runtime/integration proof: runtime truth `2026-07-10T14-17-08-009Z-runtime-truth-2b33d510`; refreshed realtime truth `2026-07-10T14-58-14-643Z-realtime-truth-4494a84f`; durable recovery `2026-07-10T14-17-58-911Z-durable-recovery-7b267550`; API compatibility `2026-07-10T14-18-28-647Z-api-compat-841fa057`; agentic contracts `2026-07-10T14-18-46-539Z-agentic-contracts-f133d06c`; agentic governance `2026-07-10T14-19-22-706Z-agentic-governance-17da9daa`; channels runtime `2026-07-10T14-21-23-018Z-agentic-channels-runtime-88608bc4`; MCP conformance 6/6; A2A full `2026-07-10T14-21-46-075Z-a2a-full-a9fd39c7`.
- Fresh coverage proof: collection `490a1aee-49b0-476e-9006-50f2b134d033` completed successfully from `2026-07-10T14:49:50.376Z` through `2026-07-10T14:55:50.079Z` against 1,409 source files. The production gate passed at 67.78% lines, 79.15% branches, and 89.65% functions versus 63%/45% required thresholds. The Gateway exercise completed its 2,322 reflective calls without a deferred-listener shutdown failure.
- Release/proof integrity: operator proof passed as `2026-07-10T14-20-30-919Z-operator-proof-730a54d6`. Refreshed architecture metrics passed as `2026-07-10T14-58-32-240Z-architecture-metrics-54f43164` with GatewayService at 8,289 lines and 896 typed host callbacks, four below the 900 baseline; no baseline ratchet was made. Final fast proof after the coverage correction passed as `2026-07-10T14-58-57-557Z-fast-9fbdc9a0`.
- Independent closure: the final read-only adversarial pass reran 131 focused lease, cancellation, continuation, provider-projection, settings, and codec tests; direct command/argv probes; Gateway typecheck; diff checks; and the architecture collector. After the hosted coverage correction, a second independent pass ran 15 realtime, approval-delivery, and events-route tests and re-proved redacted default delivery, exact-event privileged token delivery, retained/replay redaction, and idempotent wrapper cleanup. Both passes returned no remaining evidence-backed Review-02 blocker. Residual non-blocking risk is explicit: cross-process stale workers still rely on durable lease/CAS and external-side-effect idempotency, and future producer paths must continue passing the registry-issued lease.
- Review-02 local verdict: containment is safe for refreshed hosted exact-SHA checks; FR-103 intentionally remains open until the forward vault/secret-reference migration and residual sink queue are completed on subsequent serial branches.
- Residual FR-103 queue for the next security branch: forward vault/secret references and historical plaintext quarantine; memory structured metadata and maintenance output; Assembly/MatterGoat/prompt-pack/dev-verification model output; mesh replication payloads; legacy mobile audit reads; local-AI/onboarding/media/connector/voice/workspace structured diagnostics; remaining editable user-authored DTO round-trip review. These are confirmed containment gaps, not claims of closure or accepted risk.

### FR-104 — Approval effects can commit after the API reports failure

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: `approval-lifecycle-service.ts:649-689` commits resolution plus effects, then awaits fallible audit/realtime work; remote resolution consumes its token before the same fallible sequence; creation has the analogous post-commit path.
- Reproduction: inject audit failure after the transaction: the route returns 500 while the approval is resolved and its effect may execute; retry is then rejected as already resolved.
- Root cause: post-commit observability failure is represented as mutation failure, and HTTP-status-only idempotency finalization cannot distinguish commit state.
- Fix: durable audit/realtime outbox or explicit committed-result contract, with resumable remote-token claims and idempotency completion after committed mutations.
- Focused proof: post-commit failure injection; broad proof: approvals, remote resolution, durable recovery, auth matrix, and fast lane.
- Status: `fixed and proven on review-03 — canonical approval mutations commit with their events/effect envelopes; fallible follow-up work is leased and retryable; committed errors cannot revive an idempotency claim`

#### Review-03 approval commit-truth closure evidence

- Canonical ownership: generic, bulk, Chat-tool, Code Mode, device-access, improvement-activation, remote-token, and expiry paths now route approval resolution through the lifecycle or an explicit transaction owner. Approval state, linkage, events, pending-action state, wait reservations, remote-token invalidation, and durable effect envelopes commit together.
- Commit/result truth: audit and realtime delivery sit behind an idempotent observability effect lane with immutable predecessor ordering. A bounded post-commit settlement read returns the committed result or a truthful durable-follow-up-pending state; fallible audit, realtime, or settlement work cannot turn a committed mutation into an HTTP-retryable failure. Approved external actions now claim the action and run atomically, persist the external boundary before invocation, and use status compare-and-swap during stale reconciliation. Approval denial and context refresh also commit atomically.
- Race, lease, and hook proof: pending approvals, including never-polled device requests, are proactively expired. Operator resolution, expiry, duplicate remote claims, Code Mode terminalization, wait-run materialization, pending-action execution, improvement activation, and Chat cancellation/completion use transaction, lease, fingerprint, or compare-and-swap ownership. A process-local execution fence stops a stale durable worker before normal, plugin, MCP, builtin, or browser tool invocation after lease loss. `agent_end` hook idempotency includes terminal status, so a failed attempt cannot suppress a later completed/cancelled hook.
- Remote approval containment: raw `grat_...` bearers are generated only for one-time return/final transport hydration. Durable connector runs carry opaque token IDs plus OS-keychain references, reject raw bearers, bind opaque-ID resolution to the authenticated connector, enforce expiry at claim and dispatch, and retry protected-secret cleanup without replaying a successful external delivery. SQLite v139 and PostgreSQL v81 scrub historical durable, approval, audit, realtime, tool, and connector rows; frozen historical migration text was not modified.
- Chat commit and recovery truth: terminal cancellation/failure/completion uses status compare-and-swap ownership, and assistant-message ingestion shares the winning EventIngest transaction. General Chat post-commit recovery validates the linked user/assistant roles, session, turn, explicit durable run, and tool evidence before provider, memory, hook, or wait effects. UUID generations plus target-status checks prevent a stale recovery pass from clearing newer work. Operator cancellation atomically converges trace, pending input, durable run, checkpoint, timeline, and recovery marker. Stack restart proof also now calls storage transactions with the correct receiver.
- Focused and owning proof: the final five-file adversarial correction set passed 204/204 tests. Publication follow-ups passed 214/214 adjacent Gateway tests before the complete suites. Complete contracts passed 356/356; policy passed 659/659; storage passed 742 with four intentional skips; Gateway passed 5,692/5,692 Vitest plus 17/17 Node tests. Root lint had zero warnings, typecheck passed all 14 projects, the production build passed, `docs:check` passed, and `git diff --check` passed.
- Data and security proof: migration parity passed. A disposable PostgreSQL 16 instance passed 4/4 real-database cases covering async/sync clients and migrators, complete migration-ledger plus v81 scrub, nullable comms compare-and-swap, and concurrent approval observability ordering. Auth matrix `2026-07-11T08-43-53-457Z-auth-matrix-68e040e8`; security evals `2026-07-11T08-44-11-102Z-security-evals-e57c3149`; artifact redaction 5/5; MCP conformance 6/6; repository hygiene 119/119; and supply-chain proof passed.
- Runtime and agentic proof: runtime truth `2026-07-11T08-43-09-904Z-runtime-truth-64b7d12d`; realtime truth `2026-07-11T08-43-34-152Z-realtime-truth-26a9417f`; durable recovery `2026-07-11T08-57-58-286Z-durable-recovery-f8bde38f`; API compatibility `2026-07-11T09-03-08-727Z-api-compat-b74172b3`; agentic contracts `2026-07-11T08-44-30-597Z-agentic-contracts-5c69e627`; agentic governance `2026-07-11T08-44-52-838Z-agentic-governance-37889a73`; agentic proof `2026-07-11T08-45-41-607Z-agentic-proof-77b83706`; operator proof `2026-07-11T09-10-44-168Z-operator-proof-bc84f64f`; Code Mode sandbox `2026-07-11T09-10-44-167Z-code-mode-sandbox-156eed4e`; channels runtime `2026-07-11T09-10-44-167Z-agentic-channels-runtime-e62a7e69`; memory truth `2026-07-11T09-11-25-645Z-memory-truth-eb05276d`; and self-improvement trust `2026-07-11T09-11-25-809Z-agentic-self-improvement-trust-24bf19df` passed.
- Last published local broad proof: `verify:fast` passed all 14 scenarios as `2026-07-11T10-17-13-906Z-fast-5f8247c0`, including the clean-build migration-integrity lane that failed at pushed SHA `89d451009`. The accompanying Windows coverage run `6910e974-2f72-497a-8021-be0753b41f78` matched its source fingerprint but is no longer accepted as production evidence: hosted Linux measured storage at 88.63% lines instead of the Windows artifact's 97.04%. Production gating now requires Linux collector metadata, and the unchanged 90% floor must pass with margin before Review-03 closes. Historical architecture artifact `2026-07-11T10-24-19-609Z-architecture-metrics-7a101fe8` reported GatewayService at 8,453 lines, 268 public methods, 58 internal methods, 914 typed host callbacks, 279 Chat callbacks, 66 route-facing services, and no direct GatewayService route consumers. FR-267 supersedes that callback measurement: the corrected collector recomputes the same source at 917 total, and the adversarial correction set requires new full proof and an exact-SHA hosted rerun. The fast lane's machine-local wall-clock budget remained explicitly advisory and exceeded its non-strict five-minute ceiling (411.8 seconds, dominated by Gateway and storage tests); no product-performance improvement is claimed from that measurement, and the controlled variance/performance workstream remains open.
- Independent adversarial closure: the first terminal/post-commit pass returned REQUEST CHANGES after reproducing cross-session transcript and learned-memory evidence, explicit durable-run mismatch, foreign tool-wait evidence, stale marker generation clearing, and cancellation divergence. Each received a failing regression and canonical-owner fix; the rerun returned SAFE TO CLOSE with 204/204 focused tests. Earlier independent passes also closed protected remote-token hydration, hook mutation, connector binding, and cleanup races. A later cross-process pass reopened general Chat post-commit dispatch as FR-273 and dependency fan-in/resume as FR-275, so that earlier verdict does not close the current correction set. Residual risk remains explicit: provider-backed post-commit effects can only promise truthful at-least-once retry across a crash even after deterministic child-run ownership; keychain references are node-local across shared-PostgreSQL hosts; legacy already-expired rows may require observed cleanup retry; external backups and excluded audit-log files are not rewritten by the forward scrub.
- Publication-review follow-up: current review feedback was re-triaged against source rather than accepted mechanically: invalid expiry boundaries, literal `grat_` matching, bounded/resumable PostgreSQL scrubs, missing-trace recovery, and token-secret failure classification were already corrected; changed observability delivery remains an intentional immutable-idempotency conflict. New regressions make malformed reserved realtime envelopes fail closed, keep idempotent delivery independent from ambient request attribution, trim browser connector IDs, require exact-case environment allowlists, fail closed when compatibility approval creation cannot commit returned effects, guard canonical `resolveApproval` wiring, require the connector audience for every bound remote-token claim, and reject raw approval bearers in either queued interactive-action representation. Hosted Linux then invalidated the Windows-only production-coverage claim, and a second adversarial pass found additional recovery, lease, transaction, and SSE commit-truth gaps now tracked below. The audit JSONL dedupe scan remains open as FR-220 for measured indexed-deduplication work rather than receiving a partial streaming-only patch that would retain quadratic total reads.
- Reopened-slice adversarial checkpoint: the independent Gateway correction bundle passed 225/225 before the last A2A hardening; the frozen A2A/Gateway bundle then passed 100/100 and the route file 27/27. Adjacent storage passed 28/28 plus the A2A repository's 4/4. Real PostgreSQL passed the earlier four-case correction barrier and the final two A2A ownership/dispatch cases. Lint, owning typechecks, documentation governance, and diff checks were green after the final one-line test cleanup. The final read-only adversary found no remaining critical, high, or medium defect in this reopened slice; that verdict does not cover the broad Review-03 matrix or Review-04.
- Coordinated auth/proof checkpoint: ToolGrant lifecycle proof passed SQLite 13/13 and real PostgreSQL 1/1; policy passed 663/663; the two-hop approval-authority and prompt-pack TTL host guards passed 41/41. SQLite settings-auth passed 77 tests with its one conditional PostgreSQL case skipped, the storage clock helper passed 3/3, and the named real-PostgreSQL auth lane passed 66/66 with no skips. The hosted workflow now invokes that named lane and workflow validation is green. An independent auth adversary returned SAFE for this focused slice. Malformed lifecycle, vault, exchange, and active-projection cases are evidence within FR-308/FR-311 rather than separate findings.
- Final local named checkpoint: zero-warning lint, workflow validation, and `git diff --check` passed. Real PostgreSQL storage passed 18/18 and named Gateway auth passed 66/66 with no skips. API compatibility `2026-07-12T00-30-39-264Z-api-compat-5be55d4e`; auth matrix `2026-07-12T00-30-57-017Z-auth-matrix-2f178a23`; security evals `2026-07-12T00-31-12-175Z-security-evals-ad3637cf`; backup roundtrip `2026-07-12T00-31-24-851Z-backup-roundtrip-a89f19eb`; durable recovery `2026-07-12T00-31-47-634Z-durable-recovery-7e65d15f`; operator proof `2026-07-12T00-32-17-646Z-operator-proof-2d703588`; A2A full `2026-07-12T00-32-53-331Z-a2a-full-c6086269`; agentic proof `2026-07-12T00-33-08-814Z-agentic-proof-328cc8ce`; Code Mode sandbox `2026-07-12T00-35-16-293Z-code-mode-sandbox-85cc42c7`; hostile-posture canary `2026-07-12T00-35-24-552Z-code-mode-hostile-sandbox-54bfb776`; runtime truth `2026-07-12T00-35-47-588Z-runtime-truth-eb2b5e6e`; realtime truth `2026-07-12T00-36-12-428Z-realtime-truth-20ee909c`; memory truth `2026-07-12T00-36-34-406Z-memory-truth-e4228c38`; and UI parity `2026-07-12T00-36-53-695Z-ui-parity-1a236215` passed. MCP conformance passed 6/6 and the final artifact-redaction scan passed 5/5 plus the repository scan.
- Review-03 current verdict: **not safe to merge yet**. Focused/adversarial corrections, PostgreSQL barriers, the complete local named matrix, the clean source commit, and the separately committed/reverified architecture baseline are green. Those are checkpoints, not final publication proof: the final ledger commit still requires its exact-source fast/architecture rerun, followed by final-source Linux coverage and hosted CI on the exact publication SHA. Even a green Review-03 merge will not close the overall full-codebase review: Review-04, the measured-performance workstream including FR-220, the final independent adversarial pass, FR-103, and the remaining medium queue still require review or explicit disposition.

### FR-105 — A2A credentials do not enforce transport, method, or workspace scope

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: scopes are configured and returned, but `a2a-route-service.ts` dispatches JSON-RPC/HTTP without checking them; `SendMessage` accepts a peer-controlled workspace ID.
- Reproduction: a credential scoped for one transport can reach another enabled transport/method and request session/task creation in an arbitrary workspace.
- Root cause: authentication and authorization scopes are modeled separately but only authentication participates in dispatch.
- Fix: fail-closed transport/method scope matrix plus credential-bound workspace allowlist.
- Focused proof: cross-transport/method/workspace denial matrix; broad proof: `verify:a2a:full`, auth matrix, and agentic contracts.
- Status: `open — A2A authorization branch`

### FR-106 — Provider streams can truncate at EOF and still become completed turns

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: OpenAI and Anthropic SSE parsers accept EOF without required terminal events; OpenAI `response.incomplete` is ignored; completion service marks any yielded stream completed.
- Reproduction: end an OpenAI or Anthropic fixture after content/tool deltas but before `response.completed`/`response.incomplete` or `message_stop`; current collectors synthesize success.
- Root cause: parser EOF is treated as protocol completion instead of transport interruption with provider-specific terminal state.
- Fix: require terminal events, propagate incomplete reasons and partial usage, and route interruption through retry/fallback rules without replaying already-emitted output.
- Focused proof: documented terminal-event fixtures and partial tool-call tests; broad proof: provider normalization, runtime truth, and fast lanes.
- Status: `open — provider stream-truth branch`

### FR-107 — Anthropic streaming drops prompt and cache usage

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: `message_start.message.usage` is ignored and later `message_delta.usage` replaces the usage object; the existing fixture incorrectly places all usage in the delta.
- Reproduction: use the provider's documented event shape: input/cache tokens become zero while only output tokens survive.
- Root cause: usage is replaced per event instead of merged according to Anthropic's cumulative stream semantics.
- Fix: capture start usage and merge deltas while preserving input/cache fields and updating cumulative output.
- Focused proof: canonical Anthropic event fixture; broad proof: provider cost/budget and runtime truth lanes.
- Status: `open — provider accounting branch`

### FR-108 — Bulk memory forget silently stops after 500 matches

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: `listMemoryItems` clamps to 500 while namespace/query forget requests 2,000 and treats the capped response as exhaustive.
- Reproduction: seed more than 500 matching active memories and forget by namespace/query; the API reports success with matches still active.
- Root cause: a presentation-oriented list API is reused as the mutation's complete selection mechanism.
- Fix: paginate to exhaustion or perform an atomic set-based scoped mutation with truthful affected counts.
- Focused proof: >500-item forget regression; broad proof: memory truth, PostgreSQL, and fast lanes.
- Status: `open — memory lifecycle branch`

### FR-109 — Cutover can certify empty or content-mismatched imports

- Severity: `high`; confidence: `high`; release tier: `release-ops`; certainty: `confirmed`.
- Evidence: `database-cutover-service.ts` hard-codes default backup paths, normalizes missing snapshot sources to zero, imports with `ON CONFLICT DO NOTHING`, and verifies row counts only.
- Reproduction: a custom-path SQLite backup inspects zero tables and can pass; a same-key/same-count target containing different content also returns `verified:true`.
- Root cause: cutover uses pathname assumptions and cardinality as a proxy for identity/integrity.
- Fix: resolve semantic manifest roles, fail on missing required sources, and compare deterministic table/content digests before cutover.
- Focused proof: custom-path and same-count/different-content regressions; broad proof: migration parity, PostgreSQL, backup roundtrip, and install proof.
- Status: `open — cutover integrity branch`

### FR-110 — Migration immutability proof omits most current migrations

- Severity: `high`; confidence: `high`; release tier: `test-harness/release-ops`; certainty: `confirmed`.
- Evidence: protected PostgreSQL hashes stop at v28 while current history ends at v78; regex extraction silently omits v62/v63 when comments precede `version`.
- Reproduction: mutate v57 name/hash and run migration parity; the original verifier reports no immutability error and extracts only 76 records.
- Root cause: a partial manual baseline plus source-format-sensitive regex stands in for parsed migration provenance.
- Fix: parse the canonical migration arrays structurally, lock every historical version/name/hash, and reject gaps/duplicates in both dialects without changing frozen SQL.
- Focused proof: mutation/gap/comment-shape tests; broad proof: migration parity, PostgreSQL, and fast lanes.
- Status: `open — migration proof branch`

### FR-111 — PostgreSQL claims and event sequences are cross-process racy

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: mutation-idempotency claim is select-then-insert; transcript/audit logs calculate `MAX(event_sequence)+1`; PostgreSQL maps `transaction("immediate")` to plain READ COMMITTED `BEGIN`.
- Reproduction: two independent clients can observe the same absence/max; one then fails a uniqueness constraint instead of returning stable in-progress state or persisting both events.
- Root cause: SQLite locking assumptions leak through a dialect-neutral transaction label that PostgreSQL does not implement.
- Fix: atomic insert/conflict claim semantics and per-stream atomic sequence allocation or advisory locking, with real two-client PostgreSQL tests.
- Focused proof: concurrent independent-client tests; broad proof: PostgreSQL, replay, runtime truth, and fast lanes.
- Status: `open — PostgreSQL concurrency branch`

### FR-112 — PostgreSQL and custom-path backups cannot satisfy the advertised contract

- Severity: `high`; confidence: `high`; release tier: `release-ops`; certainty: `confirmed`.
- Evidence: PostgreSQL backups contain a dump plus config but verifier requires transcript/audit sections; manifest classification also recognizes only default SQLite `data/...` paths despite configurable directories.
- Reproduction: a service-shaped PostgreSQL manifest is payload-verified but contract-unverified for missing transcript/audit; valid custom-path SQLite backups fail the same classification.
- Root cause: backup roles are inferred from default relative path strings rather than backend/config-aware semantic roles.
- Fix: emit semantic manifest roles and backend-specific minimum contracts, then prove configured-path create/verify/restore and PostgreSQL dump validation.
- Focused proof: custom-path SQLite and disposable PostgreSQL variants; broad proof: backup roundtrip, PostgreSQL, and release proof.
- Status: `open — backup contract branch`

### FR-113 — Automatic Chat orchestration is absent from the canonical run projection and advertises dead controls

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: the host queries `/agentic/runs?surface=chat`, while taskless projection accepts only absent/`cowork`; projected cancel/retry controls route to a Task-backed handler and deterministically cannot find taskless runs.
- Reproduction: create a taskless Chat orchestration, list with `surface=chat`, then invoke every enabled projected control; the run is absent, or the control fails `Agentic run not found`.
- Root cause: legacy Cowork projection and Task control semantics were not normalized to the one-Chat product/runtime authority.
- Fix: include Chat-normalized taskless runs, filter before limiting, use Chat board provenance, and disable state-only controls until a canonical handler exists.
- Focused proof: mixed-surface list/tree/control integration tests; broad proof: agentic contracts/proof, runtime truth, and UI parity.
- Status: `open — agentic projection branch`

### FR-114 — One-Chat mode hides the rich workflow panel and agentic Stop control

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: real surface state always normalizes to `chat`, while workflow panel and Stop resolver remain gated on legacy `cowork`/`code`; tests mock impossible legacy modes.
- Reproduction: render the real host with an active taskless or Task-backed run; canonical Chat cannot reach the workflow panel/run control.
- Root cause: product normalization changed the mode source without changing downstream visibility predicates.
- Fix: key visibility to canonical agentic activity and executable control state, never revive separate primary Cowork/Code surfaces.
- Focused proof: real-hook Chat host/composer tests; broad proof: UI parity, surface regression, and agentic proof.
- Status: `open — one-Chat runtime truth branch`

### FR-115 — Manual Task-backed delegation lacks a durable parent continuation

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: manual delegation creates Task/run rows but no durable parent; SSE disconnect aborts execution; approval/user-input child wake has no generic parent continuation; Task cancellation lacks a live durable target.
- Reproduction: two-step manual delegation where step one waits, disconnect the observer, approve or provide input, restart, and observe missing downstream work/inconsistent terminal state.
- Root cause: streamed request lifetime owns the workflow that should be durably checkpointed and observer-independent.
- Fix: top-level durable delegation parent with idempotent child completion events, checkpointed fan-in/synthesis, passive disconnect detach, and explicit parent/child cancellation.
- Focused proof: approval and user-input disconnect/restart scenarios; broad proof: durable recovery, agentic proof, operator proof, and runtime truth.
- Status: `open — durable delegation branch`

### FR-116 — Safe demo bootstrap and install proof retain retired surface semantics

- Severity: `medium`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: the demo bootstrap still seeded separate Chat/Cowork/Code sessions while the canonical owner normalized every mode to Chat; a second bootstrap therefore accumulated five sessions instead of reusing one. The install verifier simultaneously required retired Cowork and Code primary-surface anchors.
- Reproduction: invoke safe demo bootstrap twice, then inspect canonical sessions and run `verify:install`; normalized sessions duplicate and the verifier asserts product semantics that no longer exist.
- Root cause: bootstrap identity and release proof were keyed to legacy input modes rather than the one-Chat canonical stored/runtime mode.
- Fix: seed one reusable Chat session, place agentic and governed code starter tasks inside Chat, return `/chat`, retain compatibility flags as false, and assert current product truth in install proof.
- Focused proof: demo route 9/9; broad proof: install smoke 7/7 and operator proof 3/3 on the final Review-02 source.
- Status: `fixed on review-02 — one canonical Chat demo session is idempotent and release proof no longer requires retired primary surfaces`

### FR-117 — Real PostgreSQL proof did not replay the complete migration ledger

- Severity: `high`; confidence: `high`; release tier: `test-harness/release-ops`; certainty: `confirmed`.
- Evidence: existing real-PostgreSQL tests constructed current schemas or exercised narrow migrator cases instead of applying every historical migration in order. A complete replay exposed PostgreSQL v79 SQL using the reserved alias `grant`, which the prior lane never parsed or executed.
- Reproduction: provision an empty PostgreSQL 16 database and apply migrations 1 through 80; v79 fails before current-head tests can run.
- Root cause: live database proof validated current behavior without proving that an actual historical database could traverse the full release ledger.
- Fix: add a disposable-PostgreSQL full-ledger test that applies versions 1–80, seeds representative historical plaintext rows, applies v81, and verifies the forward scrub; rename the v79 alias without changing historical schema semantics.
- Focused proof: complete-ledger PostgreSQL replay/scrub test; broad proof: real PostgreSQL suite, migration parity, storage typecheck, and fast lane.
- Status: `fixed and proven on review-03 — PostgreSQL 16 replays the complete ledger and the new forward scrub from an empty database`

### FR-118 — Remote approval bearers persisted beyond their one-time transport boundary

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: connector delivery payloads/checkpoints and adjacent approval/audit/realtime/tool rows could retain raw `grat_...` bearer values; opaque token-ID resolution did not require the calling connector; protected values could outlive expiry or terminal delivery.
- Reproduction: issue an interactive remote approval action, inspect the durable connector run and derivative rows, then resolve its opaque token ID from a different connector or leave it undispatched past expiry.
- Root cause: token generation, durable orchestration, transport hydration, connector authorization, and secret cleanup shared one raw DTO instead of an opaque-record plus protected-secret boundary.
- Fix: persist only token IDs, expiry metadata, and OS-keychain references; hydrate the bearer at the final browser/channel transport; bind opaque-ID claims to the connector; fail closed at claim/dispatch expiry; make cleanup retryable; reject raw durable bearers; scrub historical database rows with forward migrations.
- Focused proof: secret-store, connector-delivery, durable-run, opaque-claim, expiry, cleanup, and migration regressions; broad proof: auth matrix, artifact redaction, channels runtime, MCP conformance, migration parity, PostgreSQL, and security evals.
- Status: `fixed and proven on review-03 — the bearer is one-time response/final-transport data, not durable orchestration state`

### FR-119 — Chat terminal writers could commit mutually inconsistent winners

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: cancellation, background failure, non-stream completion, and streaming completion patched the same trace without a terminal ownership compare-and-swap; assistant message ingestion and trace completion used separate transactions.
- Reproduction: pause immediately before assistant-message persistence, cancel the turn, then release completion; the assistant message could commit after the trace became cancelled, or a late failure/completion could overwrite a different terminal state.
- Root cause: abort checks were advisory snapshots and terminal state/message writes lacked one transactional owner.
- Fix: add repository status-CAS support and retrying cancellation ownership, preserve existing terminal winners, propagate non-NotFound ownership errors, and execute assistant-message upsert plus the winning trace transition inside one EventIngest transaction callback.
- Focused proof: cancellation/completion/failure race tests for streamed and non-streamed turns plus EventIngest rollback tests; broad proof: Gateway, durable recovery, realtime truth, operator proof, and fast lane.
- Status: `fixed and proven on review-03 — cancellation, failure, and completion cannot overwrite another terminal winner or split message/trace commit truth`

### FR-120 — A stale durable worker could invoke a mutating tool after lease loss

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: durable lease/version checks protected state transitions and terminal writes, but normal, plugin, MCP, builtin, and browser tool dispatch could begin after another worker took over the run.
- Reproduction: pause the original worker immediately before a real `fs.copy`, expire/take over its lease, then release both workers; the stale worker performed the filesystem mutation even though its later durable-state write was rejected.
- Root cause: durable ownership was checked around orchestration state, not at the irreversible tool-invocation boundary shared by every executor path.
- Fix: issue a process-local execution fence per durable attempt and require the current owner immediately before every normal, plugin, MCP, builtin, and browser invocation; revoke it on lease loss, cancellation, or takeover without weakening durable cross-process compare-and-swap.
- Focused proof: policy/coordinator/Chat runner fence tests plus the real `fs.copy` lease-takeover integration regression; broad proof: complete policy and Gateway suites, durable recovery, security evals, and fast lane.
- Status: `fixed and proven on review-03 — a stale local worker is rejected before tool side effects, while durable lease/CAS remains authoritative across processes`

### FR-121 — Chat recovery trusted foreign transcript, run, and tool evidence

- Severity: `high`; confidence: `high`; release tier: `shipped`; certainty: `confirmed`.
- Evidence: general Chat post-commit recovery accepted stored message/tool IDs without rechecking role, session, and turn; an explicit trace durable-run ID could disagree with the durable payload; non-generation markers allowed an older pass to clear newer recovery work.
- Reproduction: link a session-A trace to a session-B user/assistant message or tool run, supply a different explicit durable run, or interleave waiting and completed post-commit generations. Foreign content could reach provider dispatch or learned memory, foreign tool evidence could satisfy a wait, and the older pass could erase the newer marker. Operator cancellation also left the trace/marker behind the cancelled durable run.
- Root cause: post-commit IDs were treated as authority instead of references requiring relational validation, and marker ownership represented only pending/not-pending rather than a specific target generation.
- Fix: validate message role/session, tool session/turn, and explicit run linkage before every effect; use only trace-linked assistant evidence; add UUID generation plus target-status checks to every effect and clear; loop reconciliation to newer generations; atomically converge trace, pending input, run, checkpoint, timeline, and marker on cancellation.
- Focused proof: cross-session/provider/memory, run-mismatch, tool-wait, stale-generation, target-status, and active/waiting cancellation regressions; broad proof: Gateway, durable recovery, realtime truth, operator proof, memory truth, and fast lane.
- Status: `fixed and proven on review-03 — foreign or mismatched evidence fails closed and each recovery generation can clear only itself`

## Confirmed Medium Queue

| ID | Finding family | Confidence / tier | Evidence and reproduction | Required fix/proof | Status |
| --- | --- | --- | --- | --- | --- |
| FR-201 | Windows path-jail validation | High / shipped | Drive-absolute, drive-relative, UNC/device paths pass `chat-project-repo` validation and become allowed roots | Shared cross-platform relative-path validator plus resolver defense; storage/tool tests | Open |
| FR-202 | Workbench process-tree cleanup | High / shipped | Timeout calls `child.kill()` and force-settles while descendants can survive | Existing process-tree killer plus descendant sentinel proof | Open |
| FR-203 | Concrete idempotency identity | High / shipped | Same key/body across distinct approval route params collides | Include canonical params and semantic query hash; route tests | Open |
| FR-204 | Stale mutation claims | High / shipped | Crash after claim leaves permanent `pending`; no lease/reconciliation | Unknown/stale state with route-owned reconciliation; restart tests | Open |
| FR-205 | Learned-memory atomicity | High / shipped | Rebuild clears before reading; item/source/conflict writes split across statements | Transactional replacement/batch mutations; failure injection | Open |
| FR-206 | Restore and cleanup atomicity | High / release-ops | Offline restore can partially overwrite; backup temp dirs leak; Chat hard-delete truncates paths and swallows cleanup failure | Staging/rollback and durable cleanup jobs; backup/session deletion proof | Open |
| FR-207 | Durable queue starvation/N+1 | High / shipped | 500 future-gated oldest rows hide ready row 501 and cause 500 retry reads | Eligibility query/index and query-plan regression | Open |
| FR-208 | PostgreSQL audit retention | High / shipped | `auditDays` prunes files only; PG audit rows remain | Backend prune capability and PG retention test | Open |
| FR-209 | Migration lineage/table quoting | High / release-ops | SQLite ignores applied-name drift; async PG migrator interpolates configurable table name | Fail-closed lineage and identifier quoting tests | Open |
| FR-210 | Provider credential cache | High / shipped | Secret rotation leaves model/auth caches keyed by raw API key | Invalidate on mutation and use non-secret fingerprint; provider tests | Open |
| FR-211 | Agentic stale tree/pagination/bounds | High / shipped | Session-only tree load, merged cursor loss, unbounded roles/steps, ephemeral fanout provenance | Run-keyed refresh, stable pagination, hard budgets, durable provenance | Open |
| FR-212 | WinUI/Tauri process and SSE lifecycle | High / release-ops | Overlapping poll children, endless launcher wait, EOF hot reconnect, watcher overlap | Single-flight/timeouts/tree cleanup/backoff/join proof | Open |
| FR-213 | Docker and installer cleanup safety | High / release-ops | `child.killed` suppresses escalation; unmarked install directories are recursively replaced | Exit-observed escalation and marker/explicit takeover guards | Open |
| FR-214 | Experimental no-agent cron bounds | High / experimental | Unbounded stdout/stderr and parent-only kill | Bounded capture and process-tree cleanup; experimental tests | Open |
| FR-215 | Approval denial/context-refresh split commit | High / shipped | State and adjacent event/context writes used separate commits, allowing failure to expose a partially applied approval outcome | Immediate transaction plus rollback/failure injection | Fixed and proven on Review-03 |
| FR-216 | Terminal hook idempotency collision | High / shipped | `agent_end` attempts shared an ID across terminal statuses, so an earlier failed hook could suppress a later completed/cancelled hook | Include status in the idempotency identity; retry/status regressions | Fixed and proven on Review-03 |
| FR-217 | Approved external claim/run crash gap | High / shipped | Action and side-effect run claims split across commits; stale reconciliation wrote from an obsolete snapshot | Atomic claim/run plus recorded boundary and status CAS; crash/takeover tests | Fixed and proven on Review-03 |
| FR-218 | General Chat post-commit recovery convergence | High / shipped | Terminal/wait effects could be lost after commit, an older pass could clear newer work, and cancellation did not converge the linked trace/marker | Durable generation markers, effect receipts, boot scan, target checks, atomic cancellation; restart/interleaving proof | Fixed and proven on Review-03 |
| FR-219 | Durable restart transaction receiver | High / shipped | Completion recovery passed `Storage.runImmediateTransaction` unbound and crashed with `Cannot read properties of undefined (reading 'db')` | Invoke through the storage owner and preserve receiver; stack-level restart regression | Fixed and proven on Review-03 |
| FR-220 | File-audit idempotency scan scaling | High / shipped/performance | Every delivery-keyed append scans the complete retained JSONL under the cross-process lock; unique deliveries produce quadratic cumulative reads | Measure representative histories, then add a durable indexed delivery-ID authority with crash/cross-process reconciliation | Open — measured performance workstream |
| FR-221 | Malformed realtime idempotency envelope degradation | High / shipped | A reserved but invalid observability envelope silently fell back to ordinary append, allowing duplicate retained events on retry | Reject malformed reserved envelopes before persistence; service regression | Fixed on Review-03 publication follow-up |
| FR-222 | Connector and environment identity normalization | High / shipped/security | Whitespace around `mission-control` produced an unusable connector binding; lowercase env names passed an uppercase allowlist but were looked up with different casing on Linux | Trim before connector canonicalization and require allowlist/lookup to use the same exact-case name | Fixed on Review-03 publication follow-up |
| FR-223 | Idempotent realtime ambient attribution drift | High / shipped | An idempotent call without captured attribution inherited request-local actors, so retry under another context conflicted with its own delivery ID | Treat missing captured attribution as explicitly empty; two-context retry regression | Fixed on Review-03 publication follow-up |
| FR-224 | Compatibility approval effects silently discarded | High / compatibility | Direct ApprovalGate fallback accepted a bare effect array but neither persisted nor delivered it | Fail closed inside the approval transaction unless the canonical creation runtime owns effects | Fixed on Review-03 publication follow-up |
| FR-225 | Migration-integrity proof depended on cached build output | High / test-harness | Clean hosted fast proof ran the source-level integrity test without `@goatcitadel/contracts/dist`; local warm trees hid the missing runtime dependency | Build contracts inside the named lane before the source test and statically enforce command order | Fixed on Review-03 publication follow-up |
| FR-226 | Connector-bound raw token claim without connector audience | High / shipped/security | The internal raw-token resolver accepted an omitted connector ID and the consumer skipped audience comparison; all shipped ingresses supplied it, but any future in-process omission could consume a bound token | Require connector identity through the internal interfaces and reject a missing or mismatched binding before claim while preserving public route compatibility | Fixed on Review-03 publication follow-up |
| FR-227 | Raw approval bearer bypass through interactive action template | High / shipped/security | The durable channel-queue guard scanned `interactiveActions` but not the separately persisted `interactiveActionTemplate.tokenRef` field | Scan both interactive-action representations before queue construction; raw-template regression | Fixed on Review-03 publication follow-up |

### Review-03 adversarial correction ledger

| ID | Severity / confidence / tier / certainty | Evidence, reproduction, and root cause | Canonical fix and focused proof | Broad proof and status |
| --- | --- | --- | --- | --- |
| FR-228 | Medium / high / shipped / confirmed | `dead_lettered` hook deliveries were not treated as terminal, so a later terminal transition could re-enter a permanently exhausted hook. Reproduce by dead-lettering an `agent_end` delivery and revisiting terminal reconciliation. Terminal-state classification omitted the dead-letter state. | Treat `dead_lettered` as terminal and cover reconciliation. Hook suite passed. | Gateway/full fast pending; fixed locally. |
| FR-229 | High / high / shipped / confirmed | Orchestration catch paths inspected only the abort signal, so a thrown durable interruption or timeout could become an ordinary failure when the signal reason was absent or replaced. Reproduce with durable interruption/timeout errors at lifecycle boundaries. Error and signal classification had diverged. | Classify from both the caught durable-control error and signal reason; loop-43 regressions passed. | Agentic/durable recovery and fast pending; fixed locally. |
| FR-230 | High / high / shipped / confirmed | Raw remote-approval bearers could still enter generic approval/action persistence outside the connector queue guard. Reproduce by placing `grat_...` in an approved-action payload handled by policy or comms. Secret containment existed only in one transport owner. | Add one shared fail-closed approved-action secret guard used by policy and comms, with prefixed/suffixed bearer regressions. Final local policy suite passed 662/662. | Auth/redaction/security/fast pending; fixed locally. |
| FR-231 | High / high / shipped / confirmed | Strict external-side-effect execution could claim and complete mutation/run truth through separate transaction owners; a crash could expose a completed external effect with incomplete durable boundary state. Legacy tests encoded the split. | Require mutation store, side-effect store, and transaction owner in strict mode; claim and complete both authorities transactionally; repair connector fixtures and recovery seed. Durable execution 68/68 and adjacent matrix passed. | Durable recovery/Gateway/fast pending; fixed locally. |
| FR-232 | High / high / shipped / confirmed | Delegated-step Chat handling converted durable interruption/timeout control signals into child failure/synthesis behavior. Reproduce by interrupting or timing out a delegated child after launch. A broad catch consumed control-plane exceptions. | Rethrow durable-control errors before ordinary child failure handling; Chat stream suite passed 44/44. | Agentic proof/durable recovery/fast pending; fixed locally. |
| FR-233 | High / high / shipped / confirmed | Durable Chat terminalization compared lease expiry to caller wall time before a database lock wait, allowing skew or wait time to make a stale worker appear current. | Add transaction-local database-clock active-lease locking and use it in terminal finalization; durable repository 28/28 and Chat finalization 21/21 passed. | Real PostgreSQL concurrency, durable recovery, Gateway, and fast pending; fixed locally, with execution-boundary follow-up FR-246 in progress. |
| FR-234 | High / high / shipped / confirmed | Unrecoverable reconciliation could overwrite a Chat trace already bound to another durable run. Reproduce with a running or completed foreign trace sharing stale payload references. The finalizer trusted an earlier snapshot. | Lock and reload the trace inside the transaction and refuse a different durable run. Focused durable recovery tests passed. | Gateway/durable recovery/fast pending; fixed locally, with complete-linkage follow-up FR-247 in progress. |
| FR-235 | High / high / shipped / confirmed | Two services could execute the same autonomous Chat post-commit callback because the marker carried no leased claim identity. Reproduce with concurrent boot recovery over one pending marker. | Add versioned claim ID/expiry, renewal, winner-only invocation, and winner-only clear. Concurrent two-service regression passed. | Gateway/durable recovery/fast pending; duplicate dispatch fixed locally; bounded cancellation remains FR-250. |
| FR-236 | High / high / shipped / confirmed | Linked workflow finalization could continue after ownership loss or hang indefinitely while its pending marker was cleared or blocked recovery. The registry exposed no abortable finalization contract. | Add `{ finalizationId, signal }`, renewal-loss abort, bounded timeout, and retain-pending semantics. Ownership-loss and timeout unit regressions passed. | Shipped wiring gap FR-249 remains in progress; not closed. |
| FR-237 | Medium / high / shipped / confirmed | Same-status Chat trace patches wrote full stale rows and could erase another writer's fields even though terminal status did not change. Reproduce two `running -> running` patches over one snapshot. | Make `patchIfStatus` lock and reload in an immediate transaction before applying the patch. Repository suite passed 15/15. | Storage/Gateway/fast pending; fixed locally. |
| FR-238 | High / high / shipped / confirmed | Approval materialization swallowed durable-run reads and committed child/parent completion, message, and trace state separately, allowing partial visible success. | Propagate durable read errors; atomically commit child and parent durable completion with message/trace state; emit realtime afterward. SQLite rollback suite passed 47/47. | Gateway/durable recovery/fast pending; fixed locally. |
| FR-239 | High / high / shipped / confirmed | Tool-grant create/revoke could commit and then return an error that released the HTTP idempotency claim; repository create also inserted and reread outside one transaction. | Mark committed mutation errors, return replay conflict without a second mutation, and make insert/read atomic. Route 22/22 and repository 8/8 passed. | Auth/Gateway/storage/fast pending; fixed locally. |
| FR-240 | Medium / high / shipped / confirmed | Proactive waiting transitions derived metadata/checkpoints from a snapshot taken before the lease-owning transaction, so a concurrent writer could be overwritten. | Lock the active lease in the immediate transaction and derive all waiting state from that locked row. Proactive 24/24 plus loop-45 4/4 passed. | Gateway/durable recovery/fast pending; fixed locally. |
| FR-241 | High / high / shipped / confirmed | Orchestration start, resume, failure, allocation failure, and linked cancellation wrote timeline entries outside the canonical durable update, so injected persistence failure split state from operator history. | Move every affected timeline event into `recordUpdate`'s durable transaction. Seven rollback regressions and the 91/91 orchestration matrix passed. | Agentic proof/operator proof/Gateway/fast pending; fixed locally. |
| FR-242 | High / high / shipped / confirmed | Proactive execution marked the irreversible boundary before policy/preflight, making a denied/preflight-failed attempt look like an unknown external outcome, while a provider failure after dispatch could still be retried. | Carry the execution fence to the canonical tool/provider invocation boundary; keep preflight failure retryable and classify post-fence provider failure as unknown/no-replay. Focused plus coordinator proof passed 97/97. | Policy/Gateway/durable recovery/fast pending; fixed locally. |
| FR-243 | High / high / shipped / confirmed | Hijacked SSE responses finalized idempotency from HTTP 200 even when send/retry/edit failed before the first canonical write; retries were blocked despite no mutation. Conversely post-commit pre-yield failures could be replayed. | Propagate an explicit mutation lifecycle from route through durable/integration/stream owners; mark at the first canonical write; release only `failed_before_commit`; synchronously persist non-durable retry trace. Ten focused files passed 153 tests. | Full Gateway/runtime truth/fast pending; fixed locally. |
| FR-244 | High / high / shipped / confirmed | Approved-action policy denial committed a relational block only after fallible JSONL audit, so audit failure left the action pending and replayable. | Commit relational policy block, pending-action failure, and approval event atomically; make this branch's file audit best effort after commit. Final local policy suite passed 662/662. | Security evals/audit/Gateway/fast pending; fixed locally. |
| FR-245 | High / high / test-harness / confirmed | Windows V8 collection reported the critical storage/policy/security tier at 97.04% lines while hosted Linux measured storage at 88.63%; the production gate accepted either artifact. Reproduce the same pushed source on both collectors. Platform-dependent collection was not represented in provenance. | Record platform/architecture/Node metadata and reject non-Linux production evidence. C8's TypeScript worker merge was also proven to collapse storage coverage; a clean forced JavaScript build restores Linux storage to 98.07% lines and keeps 751 passing plus 5 skipped tests. | Official final-source Linux collection/gate pending; platform and collector defect fixed locally without lowering floors or exclusions. |
| FR-246 | High / high / shipped / confirmed | The canonical durable Chat execution/tool fence still used `lockActiveLeaseForUpdate(..., callerNow)` even after terminal finalization moved to database time. A PostgreSQL row-lock wait or host skew can let an expired worker cross an external boundary. | Lock first through a materialized PostgreSQL CTE, check database-clock freshness after the wait, execute work, and recheck immediately before commit. SQLite and two-connection PostgreSQL regressions cover expiry during both the lock wait and work. | Real PostgreSQL 5/5, durable repository 29/29, and integrated Gateway 193/193 passed; broad proof pending, fixed locally. |
| FR-247 | High / high / shipped / confirmed | Unrecoverable durable reconciliation rejected a foreign run ID only when the trace already carried one; an unlinked trace with a foreign session/user/assistant tuple could still be failed and receive the stale run's error event. | Validate the complete trace/payload linkage tuple before any patch or event. | Focused durable recovery proof passed; broad durable-recovery/Gateway proof pending, fixed locally. |
| FR-248 | Medium / high / shipped / confirmed | Finalization locked durable then trace, while cancellation locked trace then durable; concurrent PostgreSQL transitions could deadlock. | Standardize on durable-then-trace ordering, add a transaction-local trace lock, and cover cancellation/finalization ordering. | Focused lock-order and real PostgreSQL proof passed; broad durable-recovery proof pending, fixed locally. |
| FR-249 | Medium / high / shipped / confirmed | Shipped Gateway/executor wrappers dropped the linked-finalization context even though direct registry tests received it, so timeout/ownership-loss abort signals and finalization identity did not reach real finalizers. | Forward the context end to end and require side-effectful finalizers to honor signal/finalization identity; add composition proof. | Focused Gateway composition proof passed; broad Gateway/durable-recovery proof pending, fixed locally. |
| FR-250 | Medium / high / shipped / confirmed | Autonomous post-commit callbacks renewed forever without timeout or cancellation; one hung callback blocked boot recovery and could overlap a successor after renewal loss. | Add bounded `AbortController` context, abort on renewal loss/timeout, stop heartbeat, and retain the marker for recovery. | Fake-time ownership-loss/hang proof passed; broad Gateway/durable-recovery proof pending, fixed locally. |
| FR-251 | High / high / shipped / confirmed | Approval materialization caught a transaction-local trace reread failure and continued from the caller's stale snapshot, so a transient repository error could still write an assistant message and overwrite newer trace truth. | Fail closed on child and parent trace reread errors. The red-first regression reproduced the stale assistant write; the complete approval-effects file then passed 54/54. | Full Gateway, runtime truth, and durable recovery passed; fast pending, fixed locally. |
| FR-252 | High / high / shipped / confirmed | The production runtime-host composition adapter dropped the SSE mutation lifecycle's fourth argument before `beginDurableChatRun`. A streamed retry can commit its durable run, report pre-commit failure to Fastify, release the idempotency key, and create a second run on replay. | Forward the lifecycle through the canonical host adapter. Composition regression failed with the missing argument and passed 4/4 after the fix. | Gateway/runtime truth/fast pending; fixed locally. |
| FR-253 | High / high / shipped / confirmed | The new policy secret guard scanned only `request.args`, while approval persistence serializes the broader request including `authContext.secretRefs`, attribution, consent, and policy context. A raw approval bearer outside `args` therefore remained durable; legacy persisted requests also lacked an execution-time guard. | Snapshot and scan the complete persistence-bearing request with `signal` excluded, persist that exact snapshot, and fail closed again when an approved action is executed. Red-first invoke and legacy-row regressions pass in the focused matrix; final local policy suite passed 663/663. | Security evals and auth matrix passed; fast pending, fixed locally. |
| FR-254 | High / high / shipped / confirmed | Chat prep used EventIngest's `onCommit` to mark the SSE mutation committed, but that callback runs inside the database transaction before usage, cost, outbox, idempotency, and the real commit. A later injected write failure rolls the user message back while leaving the HTTP idempotency key completed. The prep mock had encoded the opposite timing. | Add a distinct post-transaction callback while retaining the existing in-transaction callback for atomic message/trace writers; drive request-local mutation lifecycle from the post-transaction signal and add a real rollback regression. | Gateway-core 15/15 and focused Gateway 41/41 passed; broad proof pending, fixed locally. |
| FR-255 | High / high / shipped / confirmed | Even with truthful request-local timing, the canonical Chat event could commit and the process could exit before Fastify `onResponse`, leaving the persistent HTTP mutation claim pending forever despite committed Chat truth. | Complete the persistent claim inside EventIngest's canonical transaction and reserve the post-transaction hook for request-local response behavior. Rollback and post-commit-crash regressions cover both halves. | Gateway Core 17/17, focused Gateway, full Gateway, and runtime-truth proof passed; fixed locally. |
| FR-256 | High / high / shipped / confirmed | Approval materialization could complete a waiting durable run without installing fresh general/autonomous terminal post-commit generations, so approval-resumed turns skipped canonical cleanup and autonomous effects. A repeated or competing approval could also recreate or overwrite the generation. | Atomically install a generation-scoped receipt and fresh pending markers with durable completion; accept same-receipt replay, reject mismatched state, and fail closed when a competing approval wins the completion CAS. | Approval effects 54/54, durable repository 29/29, full Gateway, and durable recovery passed; fixed locally. |
| FR-257 | High / high / shipped / confirmed | Proactive tool execution could report retryable/no-side-effect truth after a nested tool or provider had crossed the real external boundary, while read-only POST preflight could be misclassified as the irreversible action. | Propagate canonical boundary truth from policy/comms execution through the coordinator and proactive result; explicitly distinguish read-only POST preflight from actual mutation. | Focused Gateway 277/277, policy 663/663, full Gateway, runtime truth, and agentic contracts passed; fixed locally. |
| FR-258 | High / high / shipped / confirmed | Orchestration cancellation could mark the projection cancelled before durable cancellation, lie when durable completion won the race, or split checkpoint/projection truth when checkpoint persistence failed. | Cancel durable first, reconcile terminal winners, and commit orchestration metadata plus checkpoint atomically so a retry repairs any durable/projection split. | Orchestration lifecycle 24/24 passed; agentic/durable-recovery proof pending, fixed locally. |
| FR-259 | High / high / shipped / confirmed | Delegated durable execution treated `DurableWorkflowTimeoutError` as an ordinary child failure, allowing timeout control flow to be swallowed and synthesized as a normal result. | Classify the timeout with the other durable control errors and rethrow it through delegation. | Red-first delegated-step regression and integrated Gateway proof passed; broad durable-recovery proof pending, fixed locally. |
| FR-260 | High / high / test-harness / confirmed | Production coverage gating trusted artifact-supplied risk-tier IDs and thresholds. A current-fingerprint Linux artifact containing one invented zero-threshold tier passed while omitting the canonical storage/policy/security tier. | Require exact gate-owned production tier IDs and thresholds and reject missing, unknown, duplicate, or lowered artifact tiers. | Coverage tooling 10/10 and the direct forged-artifact repro fail closed; final Linux collection/gate pending, fixed locally. |
| FR-261 | Medium / high / shipped / confirmed | A process exit before EventIngest begins can leave the HTTP mutation claim pending indefinitely even though no canonical side effect exists. Time-only reclaim would let a stalled original owner resume and duplicate work. | Add database-clock bounded stale reclaim with generation-token CAS; require the winning identity for failure/completion inside the canonical transaction so stale owners roll back. | Mutation repository 6/6, plugin 12/12, full storage/Gateway, migration parity, runtime truth, and real PostgreSQL passed; retry/edit parity remains FR-269. |
| FR-262 | High / high / shipped / confirmed | A delayed approval effect could seize a receipt-less durable run that had already completed normally, replace canonical output/summary, and upsert a stripped assistant message over the original parts, usage, and attachments. | Treat receipt-less completed runs as canonical terminal truth and fail closed rather than retrofitting approval ownership. | Red-first real-storage regression and focused approval proof passed; broad Gateway proof pending, fixed locally. |
| FR-263 | High / high / shipped / confirmed | Approval materialization updated durable completion before reloading an unlocked Chat trace; cancellation/failure winning between those operations could commit a completed durable run beside a non-completed trace. | Lock durable then linked trace in one transaction, validate exact linkage and terminal state before mutation, and roll back on the trace winner. | Red-first cancellation-race regression plus Gateway/storage typechecks passed; broad Gateway/PostgreSQL proof pending, fixed locally. |
| FR-264 | Medium / high / shipped / confirmed | Delegation-parent materialization used the last child approval ID as exclusive receipt identity. If child A committed then deferred on realtime while child B completed the aggregate parent, A's retry rejected B's otherwise-valid parent receipt forever. | Add a stable delegation-run/parent-turn materialization key while retaining the actual child approval ID as provenance; same aggregate retries converge and unrelated aggregates fail closed. | Red-first two-child/cross-aggregate regressions and approval effects 54/54 passed; full Gateway/durable recovery passed, fixed locally. |
| FR-265 | High / high / shipped / confirmed | Same-receipt approval retry still rewrote the completed assistant message and trace. A realtime-delivery retry after post-commit enrichment cleared parts and token/cost truth even though durable completion was idempotent. | Short-circuit canonical message/trace mutation for a matching completed receipt while still retrying realtime projection and delegation reconciliation. | Red-first enriched-message regression and approval effects 54/54 passed; full Gateway/durable recovery passed, fixed locally. |
| FR-266 | Medium / high / shipped / confirmed | Proactive approval resume trusted a stored outer `outcome: executed` even when the nested result was HTTP 5xx, `ok:false`, or manual-reconciliation-required, so failed approved actions were reported completed. | Reuse the direct-path domain-failure classifier when materializing stored approved-action results. | Parameterized 3/3 regression, focused Gateway 277/277, full Gateway, runtime truth, and agentic proof passed; fixed locally. |
| FR-267 | Medium / high / test-harness / confirmed | Architecture callback metrics counted direct `host.member` access but missed destructuring, element access, and initialized aliases; total-only comparison could also hide callback ownership moving between services. The corrected collector recomputed published source `a20384e6` at 917 total callbacks rather than 914 and `durable-execution-service.ts` at 84 rather than 81. | Count typed-host direct, destructured, element, const-alias, and mutable-alias access conservatively; follow-up baseline/provenance enforcement is tracked by FR-276 and FR-277. | Focused tooling passed 33/33. Clean source `f9d3e238` captured 913 total callbacks and 6,738 dependency-member accesses; exact-baseline architecture artifact `2026-07-12T00-46-02-800Z-architecture-metrics-c7ad8265` passed. Hosted publication-SHA proof remains pending; fixed locally. |
| FR-268 | Medium / high / shipped / confirmed | A losing strict external-side-effect claimant unconditionally wrote failure truth and could regress a winner's completed ledger row to `unknown_external_outcome`. | Use expected-status CAS for loser/failure transitions so completed truth cannot be overwritten. External runner 25/25, idempotency plugin 19/19, adjacent Gateway 67/67, and storage Node 10/10 passed. | Broad final lanes remain pending; fixed locally. |
| FR-269 | High / high / shipped / confirmed | The generation-fenced HTTP claim lease covered agent-send SSE but not retry/edit SSE, although both share the same canonical mutation lifecycle. A pre-ingest crash stranded retry/edit keys forever. | Apply the token/lease fence to all three canonical Chat SSE routes and require the current generation token at the canonical write; real-SQLite stale-owner reclaim and response-token rollback regressions cover retry and edit. The idempotency plugin passed 19/19 and the adjacent Gateway matrix passed 67/67. | Broad Gateway/runtime/final proof remains pending; fixed locally. |
| FR-270 | High / high / shipped / confirmed | Approved-action execution could retain an outer `outcome: executed` and materialize Chat success when (1) communications returned nested `status: failed`/`not_available`, (2) a generic tool returned `ok:false`, an HTTP status at least 400, or explicit error/fallback failure, (3) dispatch reported unknown-after-send/manual-reconciliation truth, (4) post-approval policy execution returned a generic block, or (5) replay loaded a previously stored executed receipt containing one of those failures. A late failure materializer could also partially rewrite a different terminal winner. | Centralize domain-result classification, reclassify executed pending-action rows to failed, and atomically materialize a truthful failed tool run, assistant explanation, child trace/durable run, and delegation-parent terminal state. Preserve manual-reconciliation metadata and fail closed against a completed/cancelled/foreign terminal winner. Eight focused Gateway files passed 198/198. | Broad Gateway, durable recovery, operator/runtime truth, and final proof remain pending; fixed locally. |
| FR-271 | Medium / high / shipped / confirmed | `agent.fanout` caught `DurableWorkflowTimeoutError` as an ordinary subtask exception, returned a failed child result, and allowed the parent to synthesize a normal partial answer instead of honoring durable control flow. | Use one shared durable-control classifier for timeout, interruption, pause, and cancellation across fanout, the agent runner, and stream boundaries; rethrow before ordinary child-failure shaping. The focused eight-file Gateway run passed 198/198. | Broad agentic/durable-recovery proof remains pending; fixed locally. |
| FR-272 | High / high / shipped / confirmed | Proactive waiting/completion and formal orchestration commits relied on pre-transaction snapshots or caller wall time instead of a database-clock lease check after lock acquisition. Proactive tool dispatch also lacked the canonical execution fence, so a stale worker could cross the external boundary after takeover. | Require `lockFreshActiveLeaseForUpdate` inside each canonical proactive/formal transaction and immediately before proactive tool execution; pass the resulting execution fence through the canonical tool coordinator. Fresh-lease, takeover, state-update, completion, and tool-boundary regressions are included in the focused eight-file Gateway run that passed 198/198. | Real-PostgreSQL concurrency plus broad agentic/durable-recovery proof remain pending; fixed locally. |
| FR-273 | Medium / high / shipped / confirmed | General Chat post-commit recovery used process-local exclusion and invoked commitments, background review, and memory maintenance after a plain parent read but before durable effect receipt. Two Gateway processes could dispatch the same generation concurrently, and a crash after a provider-backed effect but before receipt left no durable effect owner or honest retry boundary. Parent truth was also cleared when children were merely enqueued, retained realtime could be published before its receipt transaction committed, and observing a child already running on another node repeatedly woke the local zero-sleep worker loop. | Move each `(parent run, generation, effect)` to a deterministic `chat.post_commit.effect` child run claimed by the durable worker; validate its parent/generation linkage; DB-clock fence every canonical stage under the current child lease; commit database writes with their stage receipt; and reconcile the parent only after every child is terminal, recording per-effect outcomes and withholding successful completion on child failure. Retained realtime publishes idempotently after receipt commit with stable delivery IDs, so rollback cannot create a live ghost. Global background counters and workspace maintenance decisions use database row locks. Worker wakeups target queued children only; foreign running children converge through the bounded poll loop. Provider classification/suggestion reads remain truthfully at-least-once and exactly-once provider execution is not claimed. | Focused Gateway replay/reconciliation proof passed 129/129, including the 5/5 parent/restart/failure/ghost/foreign-worker integration file; SQLite owner tests passed 6/6; and the real-PostgreSQL two-child counter/maintenance contention case passed 1/1. Broad exact-source runtime, architecture, coverage, fast, and hosted proof remain pending; fixed locally. |
| FR-274 | High / high / test-harness / confirmed | Coverage collection merged Vitest/V8, c8 exercise, and c8 smoke Istanbul maps by collector-local numeric statement/function/branch IDs. Different collectors can reuse an ID for different source locations or assign different IDs to the same location, corrupting the Gateway/shared union and invalidating the earlier FR-003 percentages. | Merge statements, functions, branch groups/arms, and lines by source-location identity while preserving repeated same-location occurrences; reject missing/invalid metadata instead of guessing. Coverage tooling passes 22/22, including swapped IDs, maps, branch arms, repeated locations, unknown columns, and gate failures. | The current three-report Gateway cross-check merged 689 files with zero statement/function/branch/shape/line inventory mismatches and exact 196,266/296,409 line totals; official final-source Linux collection and production gate remain pending, fixed locally. |
| FR-275 | High / high / shipped / confirmed | Delegation persisted a step only when execution began. If an upstream step parked for approval, later dependency-plan steps did not exist durably; approval reconciliation could see only the waiting child, finish/synthesize the parent early, and restart could not reconstruct dependency order or the approved handoff. | Persist the complete normalized plan before dispatch, including stable step IDs, `parallelizable`, and `dependsOnStepIds`; resume from those rows, require every persisted plan step to reach a terminal state before parent synthesis, and durably skip downstream work when a dependency fails or is cancelled. Add forward-only SQLite v141 and PostgreSQL v83 migrations for the two plan-truth columns; frozen historical schemas remain untouched. Focused Gateway proof passed 136/136, the new resume regressions passed 13/13, the storage/migration bundle passed 55/55, and the SQLite repository passed 3/3. | The focused final delegation bundle passed 50/50, its adjacent storage bundle passed 22/22, and the coordinated real-PostgreSQL lost-update barrier passed 1/1. Contracts/storage/Gateway typechecks and scoped lint/diff checks passed at their checkpoints; broad exact-source and hosted proof remain pending, fixed locally. |
| FR-276 | Medium / high / test-harness / confirmed | The architecture baseline updater could measure a dirty Gateway/tooling tree and stamp those uncommitted metrics with the current `HEAD`, making provenance look reproducible when it was not. | Refuse baseline capture when any measured source or collector file is modified or untracked, bind the complete metric maps to a full source revision, and regression-test dirty versus clean capture. | The provisional baseline was excluded. Capture then succeeded only after measured source commit `f9d3e238`; separate baseline commit `81412ff9` and exact-baseline artifact `2026-07-12T00-46-02-800Z-architecture-metrics-c7ad8265` are green. Hosted publication-SHA proof remains pending; fixed locally. |
| FR-277 | Medium / high / test-harness / confirmed | Aggregate-only architecture comparison could miss callback ownership moving between extracted services, while typed-host aliases, destructuring, and element access escaped the earlier classifier. | Classify typed-host direct, alias, destructured, and element access conservatively and reject any per-file callback or dependency-member increase even when the total does not rise. | Architecture/coverage tooling passed 33/33; the clean baseline records per-file host and dependency maps, and its exact-baseline rerun passed. Gateway lines fell 19, total callbacks fell one, and Chat callbacks fell three versus the prior baseline; the reviewed durable-execution seam is the only per-file callback increase. Hosted proof remains pending; fixed locally. |
| FR-278 | High / high / shipped / confirmed | External-side-effect claim age, retry readiness, and stale-owner reconciliation accepted caller time, so host skew or a lock wait could replay a live mutation or strand a stale one. | Derive claim/reclaim/readiness from the database clock and fence every terminal transition with the current generation/status. | External-side-effect focused proof passed 62/62 at the frozen checkpoint; broad exact-source runtime/security proof remains pending, fixed locally. |
| FR-279 | High / high / shipped / confirmed | Durable-run claim, renewal, retry, and commit freshness mixed caller time with database-owned leases, allowing a skewed worker to cross or reject the wrong lease boundary. | Move lease and retry instants to database-clock helpers and require the exact live lease/token under lock at execution and commit boundaries. | Durable focused proof passed 78/78 at the frozen checkpoint; broad exact-source durable-recovery and hosted proof remain pending, fixed locally. |
| FR-280 | High / high / shipped / confirmed | Approval expiry plus autonomous grant/permission reserve-use-refund decisions relied on host time, so skew could reject fresh authority or consume authority that the database considered expired. | Make the repositories derive issuance, expiry, claim, and consumption from the database clock and keep deny-wins/policy checks inside the fenced mutation. | Focused storage/policy/Gateway proof and the real-PostgreSQL approval-clock matrix are green; broad exact-source auth/governance proof remains pending, fixed locally. |
| FR-281 | High / high / shipped / confirmed | Approval-effect materialization and follow-up completion could outlive their lease or split terminal truth from the durable/message/event writes they represented. | Lock the fresh effect claim inside the owning transaction, fence defer/fail/complete by worker and lease, commit relational effects atomically, and publish fallible realtime only after commit. | Focused approval-effect and rollback/takeover proof is green; broad exact-source Gateway/durable-recovery proof remains pending, fixed locally. |
| FR-282 | High / high / shipped / confirmed | Hook enqueue created the hook run, durable child, and linkage in separate steps with a non-deterministic child ID; a crash or duplicate wake could leave an unlinked row, create duplicate work, or adopt foreign linkage. | Use a deterministic child ID, lock the idempotency owner, atomically create/repair/link the hook and child, reject conflicting linkage, and treat dead-lettered delivery as terminal. | Hook rollback, repair, duplicate, foreign-link, and dead-letter regressions are green; broad exact-source Gateway proof remains pending, fixed locally. |
| FR-283 | High / high / shipped / confirmed | Remote-approval token freshness and cleanup used host time, and canonical expiry could be recorded before protected-secret deletion succeeded, leaving unrecoverable secret state or rejecting a database-fresh token. | Issue and claim against database time, keep claim/consume single-winner, delete the protected secret before canonical expiry, and leave the row recoverable when cleanup fails. | Focused skew, claim-race, cleanup, and secret-reference proof is green; broad exact-source auth/redaction proof remains pending, fixed locally. |
| FR-284 | High / high / shipped / confirmed | Approval materialization could trust stale or conflicting durable-run, Chat-trace, parent/child, and receipt linkage while committing related terminal records separately. | Lock in canonical durable-then-trace order, validate exact workspace/session/turn/run/receipt ownership, and commit linked durable, trace, message, task, and effect truth in one transaction. | Focused linkage, rollback, terminal-winner, and replay proof is green; broad exact-source durable/runtime truth remains pending, fixed locally. |
| FR-285 | Medium / high / shipped / confirmed | Relative approval and remote-token TTLs could be invalid, non-finite, or shorter than database timestamp precision, producing already-expired or ambiguous authority windows. | Validate a finite positive millisecond duration before mutation and let the database derive both creation and expiry instants from one clock observation. | Focused invalid/sub-millisecond TTL and host-skew proof is green; broad exact-source auth proof remains pending, fixed locally. |
| FR-286 | High / high / shipped / confirmed | The frozen legacy bearer scrub omitted approval-effect result/detail columns, so historical remote-approval bearers could survive in current effect truth. | Preserve historical migration text and add forward SQLite v143/PostgreSQL v85 bounded scrubs for `result_json`, `detail`, `details_json`, and `outcome`, with integrity/parity coverage. | SQLite migration/versioning and real-PostgreSQL scrub checkpoints are green; final exact-source migration-parity, backup, and hosted proof remain pending, fixed locally. |
| FR-287 | High / high / shipped / confirmed | Delegation dispatch ownership was inferred from step status and caller time, so a crashed or superseded worker could execute, fail, or settle a child after a replacement owner took over. | Persist a database-clock dispatch claim/token, require it at the child execution boundary and every terminal CAS, and repair or reuse deterministic child linkage on restart. | Focused takeover, clock-skew, crash, rejection, and replacement-winner proof is green; broad exact-source agentic/durable proof remains pending, fixed locally. |
| FR-288 | High / high / shipped / confirmed | Delegation fan-in and dependency skip decisions used stale step snapshots, allowing a recovered dependency or replacement completion to be ignored and letting parent/task/citation truth converge from mixed generations. | Lock parent, stable step set, task, and subagents in canonical order; revalidate dependency truth under those locks; derive aggregate status/citations from the locked rows; emit callbacks only after commit. | Focused dependency-recovery, active-wait, rollback, fan-in, and duplicate-wake proof is green; broad exact-source agentic proof remains pending, fixed locally. |
| FR-289 | Medium / high / shipped / confirmed | A child promise settling after timeout or abort could overwrite canonical timeout/replacement truth or surface an unhandled rejection while the failure fence was still resolving. | Track pending/won/lost settlement explicitly, buffer synchronous abort rejection until the canonical CAS resolves, and retain late success/failure only as diagnostics. | Focused timeout, late-success, late-failure, and lost-fence proof is green; broad exact-source Gateway proof remains pending, fixed locally. |
| FR-290 | High / high / shipped / confirmed | Delegation waiting, success, and error paths updated step, subagent, task, run, activity, and citations through separate writes, so an injected failure exposed partial or contradictory operator truth. | Commit each outcome under the dispatch fence in one aggregate transaction with canonical lock order; gate task activity, realtime, and memory publication on the committed winner. | Focused rollback and terminal-outcome proof is green; broad exact-source runtime/agentic proof remains pending, fixed locally. |
| FR-291 | Medium / high / shipped / confirmed | Waiting delegation could be delivered in the stream while durable run-tree/task projections lacked the same actionable output and lineage, making restart/operator inspection misleading. | Persist waiting output, child linkage, status, and provenance before stream delivery, and synthesize/repair the terminal summary exactly once from canonical rows. | Focused waiting projection, restart repair, and duplicate-wake proof is green; broad exact-source operator/realtime proof remains pending, fixed locally. |
| FR-292 | High / high / shipped / confirmed | Generic task and task-subagent updates performed full-row read/modify/write from unlocked snapshots, so concurrent status, metadata, cost, or lineage updates could silently erase one another. | Wrap updates in immediate transactions, lock and reload the row, apply only the caller patch to fresh truth, and preserve monotonic terminal fields. | Focused SQLite task/subagent suites and the coordinated real-PostgreSQL lost-update barrier are green; broad exact-source storage proof remains pending, fixed locally. |
| FR-293 | High / high / shipped / confirmed | Task-backed Chat controls labeled pause/cancel/approve/reject as applied runtime actions even where only operator state/provenance existed; approve/reject could imply an approval was resolved without invoking its canonical owner. | Keep unsupported controls state-only with no task/run status mutation, report that limitation explicitly, and direct approval resolution through the canonical approval endpoint. | TaskLifecycle focused control proof passed 63/63; broad exact-source agentic/operator proof remains pending, fixed locally. |
| FR-294 | High / high / shipped / confirmed | Delegation controls lacked one durable mutation reservation across claim, task/activity writes, and realtime; missing control IDs and post-effect failures could duplicate a control or let a stale generation complete another owner's reservation. | Normalize the semantic payload, derive stable implicit IDs for generation-bound pause/cancel retries, use DB-clock lease/token fencing, commit receipt/task/activity atomically, and probe ambiguous outcomes before retry. | Focused reservation/replay/rollback proof and the real-PostgreSQL stale-generation barrier are green; broad exact-source runtime/agentic proof remains pending, fixed locally. |
| FR-295 | High / high / shipped / confirmed | A2A cancellation targeted the agentic run ID instead of canonical task durable authority, omitted workspace/task linkage, preemptively blocked the task, swallowed lifecycle failures, derived retry identity from mutable event polling, and allowed stale full-row binding updates to regress an applied cancellation. | Atomically persist binding-to-task durable linkage; pass workspace plus A2A task identity to TaskLifecycle; fail closed on linkage conflicts; persist a stable attempt/control ID; classify thrown outcomes as ambiguous; and lock/fence binding claim/outcome transitions so applied/cancelled truth is monotonic. | A2A plus TaskLifecycle passed 78/78, A2A storage passed 4/4, and the real-PostgreSQL row-lock monotonicity barrier passed 1/1. Broad exact-source A2A/runtime and hosted proof remain pending, fixed locally. |
| FR-296 | Critical / high / shipped / confirmed | Peer-supplied A2A task IDs were treated as reusable idempotency identity before enforcing the existing binding's peer owner. A second authenticated peer could collide with another peer's task ID and receive or mutate task-linked state. | Treat the binding's peer as non-transferable authority, check it before returning or mutating any existing task, and fail every cross-peer collision closed without exposing the winner's resources. | Cross-peer read/send/cancel regressions and the reopened-slice adversarial checkpoint are green; broad exact-source A2A/security proof remains pending, fixed locally. |
| FR-297 | High / high / shipped / confirmed | A persisted `working` binding was treated as proof that Chat dispatch had happened. Restart or concurrent workers could skip a never-started dispatch or send the same inbound message more than once. | Persist separate resource-initialization and dispatch states with database-clock leases, stable session/task/turn identities, and claim-token fencing; reclaim expired work and accept completion only from the current owner. | Restart, concurrent creator, ambiguous retry, takeover, and stale-completion regressions plus final PostgreSQL A2A proof are green; broad exact-source A2A/durable proof remains pending, fixed locally. |
| FR-298 | High / high / shipped / confirmed | Legacy bindings marked `canceled` could be returned as authoritative even while the canonical task or linked durable run was still active, turning an old projection into false cancellation truth. | Reconcile legacy binding state against locked task/durable truth, cancel the durable authority first, and mark the binding canceled only after the canonical lifecycle reports an applied terminal result. | Legacy false-cancel, stale-task, and durable-first cancellation regressions are green; broad exact-source runtime proof remains pending, fixed locally. |
| FR-299 | High / high / shipped / confirmed | Distinct pause and cancel controls could both execute, then the earlier pause worker could mirror `paused` after cancellation had already won, regressing operator-visible task/run truth. | Re-read canonical durable status under the control reservation before mirroring, preserve terminal cancellation, and record the delayed pause as superseded rather than applied. | The distinct pause/cancel race and replay regressions are green; broad exact-source agentic/operator proof remains pending, fixed locally. |
| FR-300 | High / high / shipped / confirmed | Generic delegation-step patching used an unlocked stale row, while late approval materialization could patch a cancelled or replacement generation back to success/failure and resurrect obsolete child truth. | Lock/reload generic step patches; materialize approval outcome only through an exact waiting-generation CAS; and lock the parent then stable step set before aggregate mutation. | Storage patch/CAS, cancelled-step, replacement-child, and approval-lock-order regressions are green; broad exact-source storage/durable proof remains pending, fixed locally. |
| FR-301 | Medium / high / shipped / confirmed | Stable delegation run/step IDs were accepted across retries even when the caller changed dependency edges or `parallelizable`, so one durable identity could silently represent a different execution graph. | Persist and compare the normalized plan topology and parallelism on every stable wake; fail closed before dispatch when canonical graph truth differs. | Stable-plan topology/parallelism regression and the reopened Gateway bundle are green; broad exact-source agentic proof remains pending, fixed locally. |
| FR-302 | Medium / high / shipped / confirmed | Terminal replays could return before repairing stale parent/task aggregate state, and a previously written terminal summary could conflict with the repaired outcome while append-once behavior suppressed truthful correction. | Recompute and repair parent/task truth from the locked step set before stable replay; repair a missing summary once and append one explicit correction when legacy summary truth conflicts. | Terminal replay, summary repair/conflict, and duplicate-wake regressions are green; broad exact-source operator/runtime proof remains pending, fixed locally. |
| FR-303 | Medium / high / shipped / confirmed | Post-commit task activity, completion, realtime, and summary publishers shared failure classification; one publisher failure could downgrade a committed outcome or suppress later independent publishers. | Run each publisher through an isolated safe post-commit boundary, never reclassify committed outcome truth, and continue to later publishers after one observability failure. | Publisher-failure isolation and summary-continuation regressions are green; broad exact-source realtime/operator proof remains pending, fixed locally. |
| FR-304 | High / high / shipped / confirmed | Concurrent sibling approvals could each aggregate from a stale sibling snapshot and complete or park the delegation parent inconsistently because approval materialization did not serialize on the parent and complete step set. | Lock the parent first, lock/reload the stable step set, apply the exact child approval CAS, and derive the parent/task outcome only from that locked aggregate. | Focused sibling-approval proof and the real-PostgreSQL fan-in serialization case are green; broad exact-source approval/agentic proof remains pending, fixed locally. |
| FR-305 | High / high / shipped / confirmed | Production A2A send integration read a legacy top-level durable ID while canonical Chat returns `trace.durable.runId`; the two could disagree or the top-level field could be absent, linking cancellation to the wrong run or none. | Prefer and validate the nested canonical trace, fall back to the persisted turn trace when the response omits it, reject conflicting IDs, and persist the resolved durable link before declaring dispatch applied. | Nested-trace, conflicting-shadow, persisted-trace fallback, and cancellation regressions are green; broad exact-source integration proof remains pending, fixed locally. |
| FR-306 | High / high / shipped / confirmed | Concurrent peers racing one explicit A2A task ID could both create Chat/session/task resources before binding uniqueness selected a winner, leaving the losing peer's unbound resources orphaned. | Reserve peer/task binding ownership before resource creation; allow nullable links during initialization; use a database-clock resource lease and token so restart repairs only the winner's deterministic resources. | Reentrant collision, restart repair, concurrent PostgreSQL reservation, and no-orphan regressions are green; broad exact-source storage/A2A proof remains pending, fixed locally. |
| FR-307 | High / high / shipped / confirmed | A contract-valid Chat response without response or persisted trace durable truth could still mark A2A dispatch applied, leaving an apparently running task with no canonical durable authority for cancellation or recovery. | Require a resolved durable run ID before the dispatch transition becomes applied; otherwise retain the fenced dispatch claim as retryable and reuse the stable turn identity on takeover. | Missing-durable response, persisted-trace recovery, stable retry, and final PostgreSQL dispatch proof are green; broad exact-source A2A/durable proof remains pending, fixed locally. |
| FR-308 | High / high / shipped / confirmed | Device-request, device-grant, companion-session, signature-skew, and replay TTL authority used the process clock and lexical timestamp comparisons. Clock-skewed nodes could prematurely reject or extend operator-equivalent credentials; malformed PostgreSQL special values such as `infinity`, `tomorrow`, `now`, and `epoch` produced dialect-dependent fail-open behavior. | Add one database-clock owner for TTL windows and comparisons; require strict zoned ISO shape plus finite parsed time; fold fresh database predicates into issuance, grant/session lookup, rotation, validation, secure delivery, and final fences; make the plaintext vault fail closed without a valid expiry. | SQLite auth 77/77 plus storage clock 3/3 and real-PostgreSQL auth 66/66 are green, including fast/slow host skew, malformed/empty/non-finite values, valid offsets, request/grant/session row-lock boundaries, and nullable legacy grant expiry. Broad exact-source auth/security/fast proof remains pending; fixed locally. |
| FR-309 | Medium / high / shipped / confirmed | Device-request expiry resolved an elapsed approval but called the narrow resolution-effect port without `allowExpired`, so hook-shortened approvals could terminalize without materializing canonical wait-wake effects. | Widen the settings-auth effect port and Gateway composition forwarding, and pass `{ allowExpired: true }` only from the system expiry reconciler. | Poll, batch, and hook-shortened elapsed-approval regressions assert the authorized option and pass in the 77-test SQLite auth bundle. Broad approval/durable-wake proof remains pending; fixed locally. |
| FR-310 | High / high / shipped / confirmed | Companion signed-request freshness, session authority, and replay TTL were checked before potentially blocking cleanup/unique insertion; malformed replay expiry was deleted as stale. A delayed request could commit after its skew window or with an already-invalid session, while an invalid tombstone could permit nonce reuse. | Run cleanup/window/insert in one immediate transaction; delete only strictly valid expired tombstones; recheck database freshness after cleanup and insertion; require active session/grant authority in the insert and final fence; roll back late acceptance. | SQLite malformed-tombstone and host-skew regressions plus real-PostgreSQL cleanup-lock skew crossing, malformed tombstone retention, concurrent unique wait, no-late-tombstone, and replay rejection are green. Broad companion/security proof remains pending; fixed locally. |
| FR-311 | Medium / high / shipped / confirmed | Companion exchange locked grant then sessions while refresh/access used an ambiguous joined `FOR UPDATE OF s, g`; opposing acquisition order could deadlock otherwise-valid concurrent auth operations. | Pre-read token identity only, then revalidate under explicit grant-first and exact-session-second row locks for refresh and access paths; retain fresh final predicates. | Deterministic grant-before-session order regressions are green, and the native PostgreSQL rotation row-lock boundary completes without deadlock. Broad concurrency/fast proof remains pending; fixed locally. |
| FR-312 | Medium / high / test-harness / confirmed | Policy tests supplied `listActive` fakes that independently reimplemented revoked, expiry, and usage filtering. The callback therefore returned the expected answer even if policy called or interpreted repository authority incorrectly. | Prove lifecycle semantics directly through SQLite and real-PostgreSQL `ToolGrantRepository`, and make policy fakes return only already-active grants without duplicating repository rules. | ToolGrant SQLite passed 13/13, the real-PostgreSQL lifecycle case passed 1/1, and policy passed 663/663. Broad exact-source policy/fast proof remains pending; fixed locally. |
| FR-313 | Medium / high / test-harness / confirmed | The Gateway host guard proved that policy invoked a Gateway callback, but not that Gateway forwarded the exact approval authority into `approvalRuntime`; a dropped or substituted second-hop authority could leave the test green. | Add a mutation-resistant two-hop guard that captures policy callback input and asserts the same protected authority reaches the canonical approval runtime. | The focused host-guard bundle passed 41/41. Broad exact-source Gateway/policy proof remains pending; fixed locally. |
| FR-314 | Low / high / test-harness / confirmed | Prompt-pack approval tests shared one spy for persistent `create` and database-clock `createWithTtlDuration`, so the assertion could not prove which creation path the host selected. | Split the spies, assert the TTL owner is invoked with the intended duration, and assert persistent `create` remains unused. | The focused host-guard bundle passed 41/41. Broad exact-source Gateway/policy proof remains pending; fixed locally. |
| FR-315 | Medium / high / test-harness / confirmed | The conditional Gateway authentication PostgreSQL test was not part of the hosted PostgreSQL command, so local environments without the URL skipped the case and hosted verification never restored the missing proof. | Add a named `test:postgres:auth` command and an explicit hosted workflow step, preserving the PostgreSQL URL and zero-skip expectation. | Independent local execution passed 66/66 with no skips and workflow validation is green. Exact-SHA hosted execution remains pending; fixed locally. |
| FR-316 | Medium / high / test-harness / confirmed | The fast lane started the full policy and Mission Control Vitest pools together. One run starved two unrelated, normally 9-121 ms tests until they exceeded the shared 5 s timeout; the failed artifact showed 7.982 s ingestion and 10.858 s PPTX tests plus 147.65 s aggregate import time, while repeated standalone and controlled-contention runs stayed green. | Isolate policy in one awaited serial stage, then retain bounded concurrency two for Mission Control plus libraries; prove stage order and that all 14 commands execute exactly once without raising product-test timeouts. | Standalone, full-file, and controlled-contention measurements exclude deterministic product regressions; the stage-plan/adversarial bundle passed 9/9 with no high/medium residual. The full checkpoint `2026-07-12T00-21-30-401Z-fast-1e7b77e0` passed 14/14; policy completed in 10.072 s without unrelated timeouts. Final publication-SHA proof remains pending; fixed locally. |
| FR-317 | Medium / high / test-harness / confirmed | The fast timing report summed overlapping test scenarios, reporting 274,509 ms for 246,858 ms of observed test-phase elapsed time. Its first correction also trusted any parseable timestamp pair, so a forged 200 s duration with equal start/finish timestamps could report 0 ms and pass; missing tests or malformed durations could leave the non-strict lane green. | Preserve scenario intervals; use the bounded test-phase wall-clock envelope only when timestamps agree with finite nonnegative durations inside a documented adjacent-clock tolerance and valid run bounds; otherwise fall back conservatively to summed durations; propagate an explicit integrity failure for absent/invalid measurements without enforcing ordinary non-strict budget overruns. | Adversarial proof reproduced the overlap, forged interval, missing-scenario, malformed-duration, and non-strict-finalization gaps; the final shared/scenario harness bundle passed 23/23. The full checkpoint passed 14/14 in 256,824 ms and reported 219,819 ms `test_phase_elapsed`, `integrityFailure: false`; ordinary historical budgets remain advisory. Final publication-SHA proof remains pending; fixed locally. |
| FR-318 | Medium / high / test-harness / confirmed | The final-commit fast artifact `review03-final-fast-895af698` passed 13/14 scenarios but a one-shot redaction microbenchmark compared one small wall-clock sample with the immediately following large sample. A scheduler/GC interruption produced a 4.452 ratio even though the large case remained below two seconds and retained the correct 5,000 redactions. | Warm both sizes, alternate sample order, and compare median-of-three measurements while retaining the original 2 s absolute and 3.5x scaling ceilings; do not change the production redactor or isolate the test merely to conceal contention. | Production profiling remained linear: clean 30-pair median 2.025/max 2.165; 25k through 200k bytes doubled at 2.078/2.034/2.013; 12-worker contention maxed at 2.412. The hardened target passed 10/10 clean and 8/8 under 18 CPU burners, the full file passed 6/6, and contracts passed 356/356. Full final-source fast proof remains pending; fixed locally. |
| FR-319 | Medium / high / test-harness/release-ops / confirmed | Hosted fast run `29174818034` passed core fast plus both PostgreSQL lanes, then the required Linux coverage collector crashed with `Coverage map contains invalid source line undefined`. `ast-v8-to-istanbul` deliberately represents an `if` without `else` using an implicit second arm serialized as `{ "start": {}, "end": {} }`; the location-aware merger rejected that valid counter shape. | Give only the exact fully unlocated implicit arm an explicit sentinel identity, mark its whole branch collector-ambiguous so complementary reports remain conservatively separate, and retain strict locations for statements, functions, branch groups, partially located arms, and invalid non-null coordinates. | Red-first implicit-arm and malformed-shape fixtures now pass in the 25/25 tooling suite. The current local corpus contains 18,695 exact implicit arms; all 1,269 duplicate-file merges complete without numeric-ID fallback, while incomplete groups retain separate collector copies. Local Windows collection completed structurally but is not production evidence; final-source Linux collection/gate and hosted rerun remain pending, fixed locally. |

## Scope Ledger

| Subsystem | Release tier | Review status | Required closeout |
| --- | --- | --- | --- |
| Contracts and shared runtime types | shipped | Review-03 local proof green | Hosted additive-plan/API compatibility on publication SHA |
| Gateway composition and Chat runtime | shipped | Review-03 focused/adversarial and broad local proof green | Final ledger-source fast plus hosted exact-SHA proof |
| Storage, migrations, audit, and backup | shipped | Review-03 SQLite, PostgreSQL, migration, recovery, and backup proof green | Hosted PostgreSQL/coverage on publication SHA |
| Policy, approvals, tools, and Code Mode | shipped | Review-03 auth/policy/durable/sandbox proof green | Hosted canaries and exact-SHA proof |
| Orchestration, delegation, durable execution | shipped | Review-03 parity/recovery/agentic proof green | Hosted exact-SHA proof; measured-performance breadth remains Review-04 |
| Memory, skills, capabilities, and knowledge | shipped | Review-04/final cross-path pass pending | Memory truth and callable/inspectable governance |
| Providers, channels, integrations, MCP, A2A | shipped/experimental | Review-03 A2A authority/dispatch slice adversarially green; Review-04 breadth pending | Broad adapter tests, lifecycle/retry proof, and truthful degraded states |
| Mission Control Next and shared renderer | shipped | Review-04 pending | Functional, accessibility, performance, and surface proof |
| Tauri, WinUI, launcher, Docker, installers | release-ops | Review-04 pending | Native, packaging, install, hosted-platform, and workflow proof |
| Verification, coverage, benchmarks, and docs | test-harness/release-ops | Review-03 local fast, architecture, workflow, lint, docs, and redaction proof green | Final location-aware Linux coverage, named hosted lanes, measured Review-04 benchmarks, and publication evidence |

## Current Residual Risk and Remaining Program Work

- Review-03 is not publication-ready: its focused/adversarial corrections, PostgreSQL barriers, complete local named matrix, clean source commit, and separately committed/reverified architecture baseline are green. The final ledger source still needs its exact-source fast/architecture rerun, final Linux coverage, and hosted checks on the exact publication SHA.
- Deterministic durable child ownership prevents concurrent cross-process post-commit dispatch for one generation. Transactional database writes share the child stage receipt; deterministic/replay-aware artifact identity prevents duplicate semantic skill candidates; retained realtime publishes only after receipt commit and deduplicates by stable delivery ID. A provider-backed post-commit read or effect that succeeds immediately before lease loss can still be repeated on recovery, so provider cost/retry semantics remain truthfully at-least-once unless the provider honors the stable execution identity; exactly-once provider execution is not claimed.
- Residual architecture debt: A2A currently reaches broad task/durable service shapes for read-only ownership and recovery probes. The reopened runtime defect is fixed, but explicit narrow read ports remain desirable so future A2A work cannot accidentally widen mutation authority.
- FR-274 invalidated all earlier cross-collector coverage percentages. Acceptance requires a fresh final-source Linux collection and the unchanged production gate. FR-276's provisional dirty baseline was discarded; clean source commit `f9d3e238`, separate baseline commit `81412ff9`, and exact-baseline architecture rerun are green.
- Review-04 still must cover the remaining provider, memory/capability, integration/channel/A2A, UI, native/release-ops, and measured-performance workstreams. FR-103, FR-201 through FR-214, and FR-220 remain open unless a later serial branch records a proven fix or explicit owner-approved deferral.

## Positive Signals

- Current `main` is lint-, typecheck-, build-, CodeQL-, Dependabot-, and fast-lane clean.
- Coverage proof now fails closed on stale source fingerprints, non-Linux production artifacts, caller-forged tiers, and cross-collector numeric-ID merging. No final-source percentage is claimed until the location-aware Linux collection and production gate pass.
- The recent decomposition materially reduced `GatewayService` size while preserving public method counts and route-service boundaries.
- Existing verification artifacts make failures inspectable; this review will tighten what each lane is allowed to claim.

## Final Verdict

**NOT COMPLETE.** Review-03's focused/adversarial corrections, PostgreSQL barriers, complete local named matrix, clean source commit, and separately committed architecture baseline are green, but Review-03 is still not safe to merge until the final ledger source passes exact-source fast/architecture, final Linux coverage, and exact-SHA hosted checks. Review-04 and the final program-wide adversarial/publication-SHA proof pass have not been completed. The program remains open until every scope row is closed, confirmed high/medium bugs are fixed or explicitly owner-deferred, performance evidence is measured from current code, and all required final lanes pass on the exact publication commit.
