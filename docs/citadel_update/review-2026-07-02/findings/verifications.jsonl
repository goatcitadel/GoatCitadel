{"priorId":"GW-api-authz-1","status":"PARTIAL","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"integrations-telegram-routes.ts:165,172 now route the request-supplied env-var NAME through resolveAllowlistedEnvSecret (integration-webhooks-shared.ts:97) which enforces ALLOWED_SECRET_ENV_PREFIXES (:72-83). This closes the arbitrary-env exfil (DATABASE_URL, AWS_SECRET_ACCESS_KEY no longer resolve). BUT the allowlist includes prefixes 'GOATCITADEL_' and 'GC_', and the process exports highly sensitive vars under those prefixes (GOATCITADEL_POSTGRES_PASSWORD, GOATCITADEL_AUTH_TOKEN, GOATCITADEL_EVIDENCE_SIGNING_KEY, GOATCITADEL_MESH_JOIN_TOKEN, GOATCITADEL_REMOTE_APPROVAL_CREATE_TOKEN, GOATCITADEL_SLACK_OAUTH_CLIENT_SECRET). telegram-target-discovery.ts:17 still embeds the resolved value verbatim in https://api.telegram.org/bot${token}/getUpdates, so botTokenEnv:'GOATCITADEL_POSTGRES_PASSWORD' still exfiltrates that secret to Telegram. The unit comment at integration-webhooks-shared.ts:94-95 even claims GOATCITADEL_*_SECRET must not be exfiltrable, contradicting the allowlist. Mechanism narrowed, not eliminated."}
{"priorId":"XC-authz-injection-chains-1","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"gateway-service.ts:6526 still forwards request-body input.permissionProfileId to storage.permissionProfiles.resolveContext with no entitlement check; the only guards (6490, 6531) fire only when deploymentProfile==='remote_hardened'. profileMatchesContext (permission-profile-repo.ts:725-726) still returns true unconditionally for scope:global, and the builtin trusted_local_power is scope:global/approvalMode:bypass/allow:[*]. The only in-range change to permission-profile-repo.ts (listProfiles de-dups builtins from custom rows, :277) does not add any entitlement check. assertResolvedToolPolicyContextAllowed also early-returns unless remote_hardened and was not added in-range. On trusted_local/local_dev an authenticated caller can still escalate by passing permissionProfileId:'trusted_local_power'. (Was reduced to Medium; mechanism fully intact.)"}
{"priorId":"XC-authz-injection-chains-2","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"chat-delegation-service.ts is unchanged in 67c3adb64..a81eeccc4 (git diff empty). buildSubagentTaskFirstMessage still folds prior child output raw with no data-fence, and the delegation path still never calls assertNoToolOutputInjection / any promptware scanner. A researcher child that fetched hostile content can still emit directives that flow verbatim into a downstream synthesizer/reviewer prompt."}
{"priorId":"XC-authz-injection-chains-3","status":"PARTIAL","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"assembled-prompt-injection-guard.ts added 2 English-only regex markers (:6-7). But the core finding — the scanner has exactly ONE non-test caller — is unchanged: grep shows assertNoAssembledPromptInjection is still called only at improvement-service.ts:5024 (the grading prompt). The chat turn assembly still never scans the assembled prompt, so retrieved docs / skill text / file reads still reach the assembled prompt unscanned. Symptom (regex breadth) partially widened; mechanism (dead on the hot path) not addressed."}
{"priorId":"XC-authz-injection-chains-4","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"policy.ts diff only extends the PermissionProfileBuiltinId type union with 'scheduled-restricted'/'heartbeat-restricted' (:16-20). SCHEDULED_RESTRICTED_PROFILE is still toolPatterns:['*'] + approvalMode:'approve_risky' + allow:[] deny-list (policy.ts:156-158), and the engine still gates only when riskLevel!==safe. The denylist-over-wildcard vs heartbeat-allowlist drift mechanism is unchanged. (Finding was DRIFT-risk/static; no exploit today.)"}
{"priorId":"XC-authz-injection-chains-5","status":"PARTIAL","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"invokeApprovedExternalRuntimeTool (tool-invocation-coordinator-service.ts:658-755) still validates only tool-name/deployment-guard/override-handler presence then calls overrideHandler at :700 with NO evaluateToolPolicy/evaluateAccess. It remains safe only because the single caller (gateway-service.ts:6263 in executeApprovedExternalRuntimePendingAction) first runs policyEngine.executeApprovedAction(externalRuntimeReplay:true) and only proceeds on outcome==='executed', with the request rebuilt from stored pending.request. The unguarded execution primitive the finding flagged is unchanged; still safe by caller ordering only (matches original Low/no-exploit assessment)."}
{"priorId":"XC-authz-injection-chains-6","status":"RESOLVED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"executeMcpRuntime now calls enforceMcpServerScope(input) unconditionally (tool-invocation-coordinator-service.ts:927) which returns a blocked McpInvokeResponse when host.assertMcpServerInScope is absent (:1057-1064, reasonCodes mcp_capability_scope_unavailable) — no longer optional-chain fail-open. Correspondingly assertMcpServerInCapabilityScope (gateway-service.ts:7943) now throws PolicyViolationError instead of return when capabilityScopeResolver is absent, and CapabilityScopeResolver.resolve returns NO_CAPABILITIES (capability-scope-resolver.ts:93) on fault instead of ALL_CAPABILITIES. The accidental fail-open path is closed. (Note: this fix over-reached — see new regression finding.)"}
{"priorId":"XC-secrets-redaction-1","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"packages/gateway-core/src/logger.ts is unchanged in 67c3adb64..a81eeccc4 (git diff empty). SECRET_VALUE_PATTERNS (:38-41) still contains only sk- and Bearer shapes and redactSecretsInString (:43) is still the runtime log sink. The new unified redactSecretText (packages/contracts/src/secret-redaction.ts) was wired into MCP/channel/audit/elicitation paths but NOT into logger.ts. The residual gap the finding named — non-sk/Bearer secrets (ghp_, xox, AKIA, telegram bot tokens) in string VALUES under benign keys or in error .message/.stack — persists. (Medium/defense-in-depth; finding acknowledged no demonstrated E2E leak.)"}
{"priorId":"XC-secrets-redaction-2","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"packages/policy-engine/src/sandbox/network-guard.ts is unchanged in range (git diff empty). fetchGuardedOnce success-path catch at :491 still does `throw error;` unmodified, while block/redirect paths apply redactUrlForError (:73,112,132,236,241). A userinfo URL (https://bot:TOKEN@allowlisted-host/) still throws a TypeError whose .message contains the full credential; error.message still surfaces raw at integration-channel-activity.ts:229 (detail) and integration-channel-service.ts:608 (reason), both unchanged. Was CONFIRMED-REPRO Medium; mechanism fully intact."}
{"priorId":"XC-secrets-redaction-3","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"apps/gateway/src/routes/_error-handler.ts is unchanged in range (git diff empty). sendRouteError still logs log.error({ err: error }, ...) at :21 for any non-GoatError; no strengthened redactor was applied. A non-sk secret embedded in an Error.message (ghp_..., token-bearing URL) is still logged verbatim server-side. (Low, log-only.)"}
{"priorId":"XC-secrets-redaction-4","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"apps/gateway/src/dev-diagnostics/service.ts is unchanged in range (git diff empty). redactValue (:382) still redacts by KEY (REDACT_KEY_PATTERN) and string VALUES only when Bearer-shaped; it does not import redactSecretText. A token in a url/reason/detail/message value under a non-matching key still persists in the ring buffer and is handed to exporters. (Low, dev-only, no demonstrated leak.)"}
{"priorId":"GW-toolpolicy-a-1","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"packages/policy-engine/src/sandbox/argument-risk-gate.ts is unchanged in range (git diff empty). classifyArgumentRisk still applies .slice(0, MAX_ARG_VALUE_LENGTH=8192) at :30-33 BEFORE running valuePatterns (:37-44). A destructive token positioned after >8192 chars of attacker-controlled padding is truncated away and never matched, so under approvalMode:'bypass' the padded destructive call auto-executes with no approval. Was CONFIRMED-REPRO Medium; mechanism intact."}
{"priorId":"GW-toolpolicy-a-2","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"gateway-service.ts syntheticPermissionProfiles Map (:887) is still populated via .set() in registerSyntheticPermissionProfile (:5734) with NO eviction anywhere (grep for .delete/.clear/.size returns nothing). The key is `scheduled-intersection:${input.runId}` (scheduled-profile-intersection.ts:24) — unique per cron run — so a long-running gateway firing cron still accumulates one PermissionProfileRecord per run forever. (Medium unbounded growth; unaddressed.)"}
{"priorId":"GW-toolpolicy-a-3","status":"PARTIAL","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"Duplicate of XC-authz-injection-chains-5. invokeApprovedExternalRuntimeTool still invokes overrideHandler (tool-invocation-coordinator-service.ts:700) with no internal policy re-eval; the single caller (gateway-service.ts:6263) still re-gates via executeApprovedAction. Mechanism unchanged; not an independent bypass, same as before."}
{"priorId":"GW-toolpolicy-b-1","status":"RESOLVED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"createMcpServer/updateMcpServer now reject caller-supplied goatcitadel:// URLs via isInternalMcpServerUrl (mcp-server-admin-service.ts:62,104 → mcp-template-visibility.ts:31 matches any goatcitadel:// prefix, covering both durable-tasks and approval-inbox). readMcpServers (gateway-service.ts:8744) and readMcpTools (:8834) now filter out GATEWAY_OWNED_MCP_SERVER_IDS / isGatewayOwnedMcpServerUrl from caller-persisted rows and inject the Gateway-owned definitions instead; writeMcpServers/writeMcpTools also strip them. A caller can no longer register an internal server by URL to skip scope enforcement."}
{"priorId":"GW-toolpolicy-b-2","status":"RESOLVED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"handleInternalMcpDurableTasksInvoke (mcp-durable-tasks.ts:105-149) now resolves a workspace scope via resolveDurableTaskScope (:154, requires input.workspaceId else returns error — fail-closed) and filters list (:112 durableRunMatchesScope), get (:120), and cancel (:128-131) so cross-run get/cancel is rejected unless the run's workspaceId matches the caller scope. The global-list/get/cancel-over-all-runs boundary gap is closed."}
{"priorId":"GW-toolpolicy-b-3","status":"NOT_FIXED","area":"security (gateway + xcut — FW-A authz/bypass/secret/injection)","evidence":"ownerMatchesCaller (mcp-elicitation-service.ts:222-233) is byte-identical to the finding: it computes comparableKeys = keys defined in BOTH owners and returns comparableKeys.length>0 && every-match. When the caller owner is {operatorId} only and a stored elicitation owner has operatorId:undefined but agentId/sessionId set, comparableKeys is empty → returns false → the elicitation is hidden from GET /mcp/elicitations. The only in-range change to this file was swapping redactSecrets to use redactSecretText (:302), unrelated to the owner match. (Low correctness.)"}
{"priorId":"GW-concurrency-1","status":"RESOLVED","area":"orchestration","evidence":"Fix closes the check-then-act TOCTOU on the autonomy kill switch. New assertAutonomousDurableRunAllowed() gates the durable EXECUTOR dispatch (the choke point that runs when a run is dequeued, long after enqueue) for proactive.tick at apps/gateway/src/services/durable-execution-service.ts:591, connector delivery :1063, chat.turn :1189; plus a defense-in-depth re-check inside executeDurableProactiveTickRun at apps/gateway/src/services/chat-proactive-service.ts:2165; plus enqueue-side gates in maybeEnqueueAutonomousDelivery :1311 and enqueueAutonomousChatTurn (gateway-service.ts:5761). A run enqueued while autonomy was on is now blocked at execution time once the operator engages autonomyV1Disabled. Regression tests added at durable-execution-service.test.ts:708/1026/1531. Verified: the isFeatureEnabled host is wired at gateway-service.ts:1686/1712. (Note: this fix introduced a lost-run regression — see new finding R2-orchestration-1.)"}
{"priorId":"GW-orchestrator-a-1","status":"NOT_FIXED","area":"orchestration","evidence":"apps/gateway/src/services/chat-agent-completion-adapters.ts:398-401 is byte-identical to the pre-fix code: readToolCalls(message,...) result from choice.message only sets sawToolCall=true and is never copied into aggregate.toolCalls; buildCompletionFromAggregate (:475-500) builds tool_calls solely from aggregate.toolCalls (populated only by the delta path :446), so message-path tool calls are dropped and the orchestrator sees zero calls -> terminal 'completed' with no tool run. `git diff 67c3adb64..a81eeccc4 -- chat-agent-completion-adapters.ts` only added <tool_call> JSON parsing to parseSerializedToolCalls; grep for fullToolCalls/sawToolCall/aggregate.toolCalls in the range diff returns nothing. chat-agent-orchestrator.ts (the real consumer at :2311) is unchanged in the range. Proven with a repro test exercising absorbCompletionStreamChunk+buildCompletionFromAggregate (aggregate.toolCalls.size===0, built message tool_calls undefined)."}
{"priorId":"GW-orchestrator-a-2","status":"NOT_FIXED","area":"orchestration","evidence":"apps/gateway/src/services/chat-agent-completion-adapters.ts:48-76: readToolCalls still enters `if (Array.isArray(raw))` for an empty tool_calls array, loops zero times, and returns out=[] at :75 WITHOUT falling through to parseSerializedToolCalls(extractMessageContent(...)) at :77. inspectToolCallProtocolIssues (:84-87) likewise returns [] for [], so repair never triggers. The remediation only added <tool_call> JSON parsing INSIDE parseSerializedToolCalls (:195-227) — unreachable when tool_calls===[]. chat-agent-tool-call-repair.ts is unchanged in the range. Proven with a repro: readToolCalls({content:'<function=search_web>...</function>', tool_calls:[]}, canonical) returns [] and inspectToolCallProtocolIssues returns []."}
{"priorId":"GW-turn-pipeline-1","status":"PARTIAL","area":"orchestration","evidence":"apps/gateway/src/services/chat-turn-prep-service.ts is UNCHANGED in the range (empty git diff): line 358 still `effectiveMode = quickWebTurn ? normalized.mode : prefs.mode` (prompt) while :595 and :669 use `normalized.mode ?? prefs.mode` (routing/runtime). normalizeAgentInputFromSend (chat-agent-input-normalization.ts:35, also unchanged) always returns `mode: request.mode ?? 'chat'`, so `?? prefs.mode` is dead and routing collapses to request.mode??'chat'. Repro confirmed: for a bare turn, normalized.mode==='chat' while prefs.mode='cowork' -> prompt=cowork, routing=chat. PARTIAL because the remediation hardened only the AUTO-ROUTE entry path: surface-router-entry.ts:30-32 now reads the persisted sticky mode and injects it into input.mode (making normalized.mode match prefs.mode) — but this runs only when input.autoRoute is set (guard :27). For a sticky-cowork session with no mode and no autoRoute (the exact finding scenario), input.mode stays undefined into prep and the divergence persists."}
{"priorId":"GW-streaming-1","status":"RESOLVED","area":"streaming","evidence":"apps/gateway/src/routes/chat.shared.ts:100-113 (added by a81eeccc4 'fix: close streaming findings') introduces a 15s setInterval heartbeat writing ': heartbeat\\n\\n' SSE comments during silent gaps, directly closing the finding's 'no heartbeat timer -> idle proxy drops SSE' mechanism. It is guarded (line 101: skips if closed||raw.destroyed||raw.writableEnded||signal.aborted), cleaned up via stopHeartbeat() from cleanup() (line 72) and the finally block (line 159), unref()'d (line 113), and calls cleanup() if the heartbeat write fails/rejects (lines 106-111). New test chat.shared.coalesce.test.ts:75-115 proves an idle stream emits ': heartbeat\\n\\n' after CHAT_SSE_HEARTBEAT_INTERVAL_MS and ends exactly once on 'close'. Focused suite (chat.shared.coalesce + chat-turn-stream-service) = 33/33 pass on tip."}
{"priorId":"GW-streaming-2","status":"RESOLVED","area":"streaming","evidence":"No fix was required or made — the prior finding self-classified this as 'verified intended, corroboration only ... not a defect'. apps/gateway/src/services/chat-turn-stream-service.ts is byte-for-byte unchanged in 67c3adb64..a81eeccc4 (git diff = 0 lines). Current code at chat-turn-stream-service.ts:1747 still guards `if (!streamedRealFinalDeltas)` before the synthetic splitIntoChunks(finalText,120) split (line 1748), so the happy path (live terminal deltas matched authoritative finalText, computed at :1634-1635) emits NO synthetic split = no double emit; the transient double-render only occurs on the divergence/recovery path where message_done (content=finalText, :1759-1763) authoritatively REPLACES the mismatched live text. Behavior matches the finding's intended-tradeoff assessment; no regression."}
{"priorId":"GW-storage-correctness-1","status":"RESOLVED","area":"storage-memory","evidence":"chat-message-repo.ts:326-342 replaces the unconditional `SAVEPOINT` (which crashed on Postgres with 'SAVEPOINT can only be used in transaction blocks') with `withAtomicBatchWrite`: SQLite keeps the raw savepoint path (l.331-341), non-sqlite routes through `this.db.transaction(\"immediate\", work)`. The synchronous Postgres DatabaseClient (postgres/sync.ts:70 PostgresSyncDatabaseClient.transaction) issues tx_begin/tx_commit/tx_rollback via the worker, and sync-worker.ts:130 maps tx_begin to a real `BEGIN` on a dedicated pooled client, so upsertMany is now atomic on both dialects. Same fix applied to cost-ledger-repo.ts:203 and operator-profile-repo.ts:114. Focused node:test suites pass: chat-message-repo.search.test.ts 9/9, chat-message-repo.test.ts + defensive-tail + transcript + permission 69/69, postgres-sync/transcript/runtime-schema 56/56."}
{"priorId":"GW-memory-1","status":"RESOLVED","area":"storage-memory","evidence":"memory-context-service.ts:49-52 compose() now returns `this.degradeUnsafeMemoryContext(input, pack)` instead of the base `assertNoMemoryContextInjection(pack.contextText)` that THREW (base 67c3adb64 l.48). degradeUnsafeMemoryContext (l.613-655) runs sanitizeMemoryContextWrite (memory-context-safety.ts:39) which scans via scanPromptwareContent; on a hit it returns a degraded fallback pack (context withheld, citations:[], quality.status='fallback'), appends a memoryQmdRuns 'fallback' row with errorText=reason:evidenceHash, and publishes 'memory_qmd_fallback' — an honest degradation, not an uncaught throw / lost turn. Generated path also sanitized inline at l.306-336. Test 'withholds compiled memory context that contains prompt injection' passes."}
{"priorId":"GW-memory-2","status":"NOT_FIXED","area":"storage-memory","evidence":"candidate-ranker.ts:133 still computes BM25 term frequency with `token === term || token.includes(term)` — byte-identical to base 67c3adb64:132 (git diff --stat shows the only change in this file is the maxCandidates clamp at l.71-72 from commit 106026856). The substring match still inflates TF and DF (docFreqs[j]+1 at l.141): a short query term like 'ai' matches 'email'/'detail'/'maintain'/'campaign', and 'cat' matches 'category'/'concatenate', polluting the ranking the finding flagged. No new test asserts exact-token matching (memory-core 27/27 pass but none exercise the substring path). Note the same substring bug persists in semanticHintScore l.97 (`hint.includes(term)`). Mechanism fully intact."}
{"priorId":"GW-memory-3","status":"RESOLVED","area":"storage-memory","evidence":"The compose/distiller prompt is bounded at every layer in current code: collectMemoryCandidates uses maxCharsPerCandidate:1400 (candidate-collector.ts:139-143 truncates with a marker), the ranker slices to maxCandidates:40 (candidate-ranker.ts:72), collectSources caps files/memory items at maxMemoryFiles:36 and transcript events at maxTranscriptEvents:80 (memory-context-service.ts:130-147,536-578), and the distiller max_tokens is Math.min(1400,...) (l.264). Worst-case evidence is ~40x1400 chars. buildDistillerPrompt (distiller.ts:9) only joins the already-bounded candidates. Caveat: these bounds are byte-identical to base (no new cap was added in 67c3adb64..a81eeccc4), so if the finding targeted these exact limits it was already mitigated before this round; the current compose prompt is demonstrably bounded either way."}
{"priorId":"GW-memory-4","status":"RESOLVED","area":"storage-memory","evidence":"citation-validator.ts:8-25 upgraded from a candidateId-membership Set to a Map and now rejects a citation unless sourceType, sourceRef match the candidate AND score is finite and in [0,1] — closing the gap where a distiller could cite a valid candidateId with a fabricated sourceRef/score. Additionally memory-context-safety.ts:findInvalidFactCitationIds + memory-context-service.ts:296-299 now throw when any fact.citationIds reference a citation the distiller did not return, forcing the catch->fallback path. Test 'falls back when distilled facts reference citations the distiller did not return' passes; distiller.ts:69 also clamps parsed score to 3dp."}
{"priorId":"P3.T1","status":"NOT_FIXED","area":"storage-memory","evidence":"cowork-agentic-projection-service.ts is byte-identical between base 67c3adb64 and fix tip a81eeccc4 (git diff --stat empty). getAgenticRunTree (l.103, invoked from the GET route tasks.ts:579) still performs storage writes during a read: reconcileParentDurableTrace calls chatTurnTraces.patch (l.405), reconcileRunSteps calls chatDelegationRuns.patch (l.445), reconcileStep calls chatDelegationSteps.patch (l.483). The remediation plan (docs/superpowers/plans/2026-07-02-findings-remediation.md l.34) lists 'Cowork GET reconciliation writes' (GW-orchestrator-b-2/GW-concurrency-3/XC-perf-system-3) as covered, but no fix commit touched this file. Pre-existing idempotency guards (l.400 skip-when-status-matches, l.437 P3-T4b reload-before-patch, one-time 'running'-only transition) predate the range and limit blast radius, so real-world severity is low — but the write-on-read mechanism the finding named is unchanged this round."}
{"priorId":"MC-chat-render-markdown-1","status":"RESOLVED","area":"mc-rendering","evidence":"Fix at packages/mission-control-shared/src/components/chat/assistant-display-text.ts:216 (preserveMarkdownIndentation) replaces the old blanket `.map((line) => line.replace(/[ \\t\\f\\v]+/g, \" \").trimEnd())` (previously ~line 121) that collapsed ALL whitespace incl. leading indent. New code preserves leading indent (spaces+tabs) and only collapses interior 2+ space runs, and swaps the naive FENCED_CODE_BLOCK_RE split for a proper line-based splitMarkdownFenceSegments (line 126) that also handles ~~~ and indented fences. Verified live: normalizeAssistantDisplayText('Plan:\\n  - a\\n    - b') returns indentation intact; and chat-renderer-tail.test.tsx:208 'preserves markdown indentation and fenced code' passes. Note Review1's 'did-not-repro-live' still holds for the old symptom, but the fix is a genuine correctness improvement (nested-list/code indent now survives). Ran chat-renderer-tail.test.tsx: 21/21 pass."}
{"priorId":"MC-chat-render-markdown-2","status":"RESOLVED","area":"mc-rendering","evidence":"Fix at packages/mission-control-shared/src/components/chat/AssistantMessageRenderer.tsx:498 (hasSameLineStreamingFenceClose) applied to BOTH splitStreamingMarkdown (line 450) and the incremental splitIncremental (line 608). Prior bug: a single-line fence like ```json {\"ok\":true}``` was treated as OPENING an unclosed fence, so every following paragraph was pinned in the unstable 'tail' forever (streaming never stabilized). Now the opener is checked for a same-line close and does not enter fence mode. AssistantMessageRenderer.incremental.test.tsx proves byte-identical per-character equivalence with fixtures sameLineFence/indentedSameLineFence/unclosedTildeFence/unclosedBacktickFence; chat-renderer-tail.test.tsx:230 asserts stable='intro\\n\\n```json {\"ok\":true}```\\n\\n', tail='outro tail'. Also fixed isMarkdownCodeBlock (line 146) to use AST node position. 21/21 tests pass."}
{"priorId":"MC-state-hooks-2","status":"RESOLVED","area":"mc-rendering","evidence":"Fix at packages/threaded-surface-core/src/chat/useChatSessionData.ts: loadSessionCoreStateInternal now returns false on stale generation (line 367) / true on apply (line 373); loadSessionState bails before firing the secondary load for a superseded session (lines 451-454, `if (!coreLoaded) return;`). The selection effect now bumps BOTH loadCoreGenerationRef and loadSecondaryGenerationRef on clear (lines 604-605) and on session switch (lines 611-612), invalidating any in-flight load from the prior session so a late poll/deferred-secondary can't clobber the newly selected session. Prior code (base 67c3adb64 lines 353,425) captured the generation but loadSessionState unconditionally continued to loadSessionSecondaryState even when core was stale. New regression test useChatSessionData.test.tsx:450 'ignores stale selected-session loads after the selection clears' passes (thread/prefs stay null, applyFetchedThread not re-invoked after clear). Suite: 18/18 pass. Immediate post-switch load is not self-invalidated because loadSessionCoreStateInternal takes a fresh ++generation at line 354 after the effect's bump."}
{"priorId":"MC-virtualization-perf-1","status":"RESOLVED","area":"mc-rendering","evidence":"Two-part fix. (1) packages/mission-control-shared/src/state/channel-activity-store.ts:120 adds a per-message selector useChannelActivitySnapshot + scoped listeners (subscribe carries {sessionId,messageId}, line 129) so notifyChannelActivityListeners(changed) only wakes the badge whose scope matches the changed snapshot (shouldNotifyChannelActivityScope, line 155) — killing the O(n^2) wake-storm where every visible turn re-ran `.find` over ALL snapshots. Publish now dedups no-op updates (areChannelActivitySnapshotsEqual, line 89-91) and getChannelActivitySnapshotForMessage returns a stable object/null ref satisfying useSyncExternalStore. apps/mission-control-next/.../ThreadedTimeline.tsx:53 switches TurnChannelActivityBadge from useChannelActivitySnapshots+useMemo(.find) to the scoped hook. (2) ThreadedTimeline.tsx:255 resolveStreamingPreviewScrollSignal buckets visibleText length (Math.floor(len/128)+line count) so the auto-follow scroll effect no longer re-fires on every streamed character. ThreadedTimeline.test.tsx (29/29 pass) asserts unrelated-message activity does not render, same-bucket signal ref-equal, cross-bucket differs. ChatThreadPrimitives.tsx:369 also adds useEffect to reset TurnEvidenceSummary open-state on turn.turnId change (recycled-row correctness)."}
{"priorId":"MC-a11y-3","status":"RESOLVED","area":"mc-shell-wip","evidence":"apps/mission-control-next/src/features/threaded-surface/workflow/CoworkPanel.tsx:420-471 — the fix DELETED the focus-steal (removed approvalResolveButtonRef and the queueMicrotask(()=>approvalResolveButtonRef.current?.focus()) at old :457) and REPLACED it with a polite live region: added approvalAnnouncement state (:420) rendered in <p className=\"mc-next-sr-only\" role=\"status\" aria-live=\"polite\" aria-atomic=\"true\"> (:469-470); the effect now only scrollIntoView + setApprovalAnnouncement (:461-465). mc-next-sr-only is defined (native-routes.css:1096). Closes the finding: approval arrival no longer yanks focus from the composer and now has a paired live-region."}
{"priorId":"MC-a11y-4","status":"RESOLVED","area":"mc-shell-wip","evidence":"apps/mission-control-next/src/features/threaded-surface/workflow/CoworkPanel.tsx — tabIndex={0} added to all four tabpanels: plan (:192), run-map (:213), timeline (:219), actions (:225). A keyboard user can now focus/scroll a panel that has no focusable child (e.g. idle Plan)."}
{"priorId":"MC-css-tokens-1","status":"RESOLVED","area":"mc-shell-wip","evidence":"apps/mission-control-next/src/app/MissionControlNextApp.tsx:1334 builds accessibleLabel=`${label}: ${value}${degraded?' (unavailable)':''}` and applies it as aria-label on the non-clickable footer pill <div> (:1350). Footer pills pass explicit label props (Sessions/Spend/etc., :1085-1096) so the accessible name is now e.g. 'Sessions: 200 visible' not a bare value. Note: base 67c3adb64 already rendered both <span>{label}</span><strong>{value}</strong> as text (the 'bare value' premise was the pre-base WIP state); the fix makes the label-first name explicit. Minor caveat: aria-label on a role-less <div> is inconsistently exposed by AT, but the label text content already provided the name."}
{"priorId":"MC-css-tokens-2","status":"RESOLVED","area":"mc-shell-wip","evidence":"apps/mission-control-next/src/app/MissionControlNextApp.tsx:1342 — the clickable Approvals footer pill (onClick branch, rendered as <button>) now gets aria-label={accessibleLabel} = 'Approvals: 0 pending'. Buttons reliably expose aria-label, so the accessible name is no longer just the numeric value. Approvals StatusPill at :1078-1084 passes label=\"Approvals\"."}
{"priorId":"MC-wip-changeset-2","status":"RESOLVED","area":"mc-shell-wip","evidence":"Route/model + tokens/cost no longer vanish under composer-v2='off'. Fix exported computeUsageTotals/formatTokenLabel/formatCostLabel from ThreadedComposer.tsx:62-93 and renders them as always-visible header StatusChips in ThreadedSurfacePage.tsx:761-795 (Model/Route/Runtime/Tokens/Cost/Approvals) regardless of the composer-v2 flag. The composer v2-off fallback chip-row (ThreadedComposer.tsx:770-802) also still shows routeLabel(:793) + usageLabel(:794). (The chip-row fallback was in fact already present at base 67c3adb64:793-794; the fix's header chips are the durable fix.)"}
{"priorId":"MC-wip-changeset-3","status":"RESOLVED","area":"mc-shell-wip","evidence":"onModeOverride is wired and reachable, not dead. MissionThreadedControllerHost.tsx:3168-3171 sets onModeOverride to () => { setModeOverride(mode); onResolvedModeChange?.(mode); } (URL sync intact). ThreadedModeControl.tsx:98 now renders the interactive dropdown when interactive!==false && onOverride && !disabled; the conversation header (ThreadedSurfacePage.tsx:753-757) passes a real onOverride={props.onModeOverride} with no interactive={false}, so the manual-override menu (items call props.onOverride at :145) is live. The fix also correctly falls back to a read-only readout when onOverride is undefined (removed the dead disabled trigger)."}
{"priorId":"MC-wip-changeset-4","status":"RESOLVED","area":"mc-shell-wip","evidence":"The Cowork Plan tab is not blank when idle: CoworkPanel.tsx:194-208 renders three PanelLists each with emptyCopy, and PanelList.tsx:19+33-34 renders the title kicker unconditionally plus <p>{emptyCopy}</p> when items.length===0. emptyCopy is NOT dead. (This was already the case at base 67c3adb64 — the fix additionally added tabIndex={0} at :192 for MC-a11y-4.) Merged root with MC-a11y-4 per plan."}
{"priorId":"MC-css-tokens-3","status":"NOT_FIXED","area":"mc-shell-wip","evidence":"composer.css was never touched in 67c3adb64..a81eeccc4 (git log --oneline for the file is empty). The stale guard at apps/mission-control-next/src/features/threaded-surface/styles/composer.css:954 `.mc-next-composer-delegation-head > span:not(.status-chip)` sets color:var(--text-secondary). The delegation-approval alert renders the PRIMITIVE StatusChip (ThreadedComposer.tsx:354 <StatusChip tone=\"warning\">Subagent approval</StatusChip>) which emits class=\"mc-next-status-chip\" (native-routes/primitives/StatusChip.tsx:18) — NOT '.status-chip' — so :not(.status-chip) MATCHES it. Specificity: composer rule (0,2,1) beats the tone rule .mc-next-status-chip[data-tone=\"warning\"] (0,2,0) at primitives.css:496, and the label span (primitives.css:480) sets no color so it inherits the muted override. Warning text tone is still clobbered (border/bg survive)."}
{"priorId":"MC-responsive-1","status":"NOT_FIXED","area":"mc-shell-wip","evidence":"The finding's subject is the shell inspector 'Context SideInspectorDrawer' (side-panels.css cited; actual bottom-dock rule is mission-control-next.css:2187-2195 `top:auto; bottom:0.9rem` @media (max-width:1180px)). Neither mission-control-next.css nor packages/mission-control-shared/src/components/SideInspectorDrawer.tsx was touched in the range (git log empty for both). The inspector still bottom-docks over the lower viewport at <=1180px. The fix instead targeted the LEFT session rail (mc-next-threaded-rail, mobile.css:155-198 + ThreadedSurfacePage.tsx:346-360), which is a slide-in list, not the 'Context' drawer and never bottom-docks. Nuance: the shell inspector already had a scrim (MissionControlNextApp.tsx:1042 + CSS 2197) and Escape via the pre-existing H-7 handler (MissionControlNextApp.tsx:263-280,291-299), both present at base, so the 'no scrim/no escape' harm was not real at base; the bottom-dock-over-composer behavior persists unaddressed."}
{"priorId":"MC-responsive-2","status":"NOT_FIXED","area":"mc-shell-wip","evidence":"SideInspectorDrawer.tsx (the draggable shell inspector) was not modified in the range. The drag guard still reads window.innerWidth < 1024 (SideInspectorDrawer.tsx:109,138) while the CSS bottom-dock breakpoint is <=1180px (mission-control-next.css:2180), and at <=1180 the docked rule sets transform:none (:2194) which overrides the drag transform (:1379). So in the 1024-1180px band grabbing the header still 'does nothing'. The fix touched the unrelated left session rail, which is not draggable."}
{"priorId":"MC-responsive-3","status":"NOT_FIXED","area":"mc-shell-wip","evidence":"On desktop the shell inspector is position:fixed; right:0.9rem (mission-control-next.css:1363-1366) and the right-gutter reservation .has-shell-inspector ... padding-right (mission-control-next.css:1358-1360) only applies to .mc-next-body:not(.is-work-area) at min-width:1181px. The chat/cowork/code surfaces ARE is-work-area (MissionControlNextApp.tsx:333), and the 'Open Context' topbar button (:888-897) toggles inspectorOpen unconditionally, so an open shell inspector floats over the work-area conversation column with no reserved gutter. mission-control-next.css was untouched in the range; the fix addressed the session rail instead. (Live confirmation of shell-inspector-content on chat is a QA1 item.)"}
{"priorId":"MC-a11y-1","status":"NOT_FIXED","area":"mc-shell-wip","evidence":"packages/mission-control-shared/src/components/SideInspectorDrawer.tsx was NOT modified in 67c3adb64..a81eeccc4 (empty diff). It still has no move-focus-on-open, no focus-restore-on-close, no role=\"dialog\"/aria-modal, and no inert on the background (grep for inert/role/focus( in the file returns only the drag-target closest() at :113). At <=1180px it is a scrimmed bottom sheet (visually modal) with no focus management. The fix added focus move/restore only to the LEFT session rail (ThreadedSurfacePage.tsx:293-345), a different component; the cited SideInspectorDrawer is unchanged."}
{"priorId":"MC-a11y-2","status":"NOT_FIXED","area":"mc-shell-wip","evidence":"The cited line ThreadedSurfacePage.tsx:583 at base was inside the RIGHT Context panel <aside className=\"mc-next-threaded-context-panel\"> (dockOpen). In current code that panel (ThreadedSurfacePage.tsx:644-677) still has no Escape handler — the only new Escape effect (ThreadedSurfacePage.tsx:346-360) is gated on railDrawerOpen and closes the LEFT session rail, and ThreadedPanelSwitcher's Escape (:1077) only closes its dropdown. So the chat right Context drawer cannot be closed with Escape. The fix added Escape to the wrong drawer (left rail). Mitigation: the panel is position:static/stacked at <=1180 (mobile.css:86), not a focus trap, and is dismissable via the visible 'Hide context' button (:592-597) — so harm is low, but the finding is not resolved."}
{"priorId":"XC-config-drift-1","status":"NOT_FIXED","area":"platform-config-xcut","evidence":"packages/policy-engine/src/policy-resolver.ts had ZERO changes in 67c3adb64..a81eeccc4 (git diff empty). The fail-open branch is still present verbatim at policy-resolver.ts:24-26 — `: profileName ? new Set(config.profiles?.[profileName] ?? []) : new Set<string>([\"*\"])` — so an absent profileName still yields effectiveTools={*}. No default-profile injection was added at the Zod/contract layer either: packages/contracts/src/policy.ts:16 only expanded PermissionProfileBuiltinId (added scheduled-restricted/heartbeat-restricted) and does not inject a default `tools.profile`. The mechanism (absent=fail-open vs unknown=fail-closed inconsistency) is unchanged. Shipped-config safety is preserved incidentally: config/tool-policy.example.json:3 still sets `\"profile\":\"chat-agent\"`, so profileName stays truthy and the shipped config does not hit the wildcard — matching the finding's Medium rationale. Latent/operator-drift risk unchanged."}
{"priorId":"XC-config-drift-2","status":"RESOLVED","area":"platform-config-xcut","evidence":"config/goatcitadel.example.json (git diff 67c3adb64..a81eeccc4, lines 211-259) removed the dead 7-profile `profiles:{minimal..danger}` map and replaced the empty `tools.allow:[],deny:[]` with the SAME rich allow/deny arrays as the split config/tool-policy.example.json — so the unified file no longer carries dead reference config. apps/gateway/src/config-defaults.test.ts:76-80 (new) now asserts unified.assistant===split assistant AND unified.toolPolicy===JSON.parse(tool-policy.example.json) AND unified.budgets/cronJobs/llm===their split files, a deep-equal drift gate that fails CI if they ever diverge again. I ran the suite: 4 files/8 tests pass. Caveat (not a regression): the reconciliation codifies the split's behavior, so a fresh chat-agent still cannot browser.search (engine.ts:750-751 hard-denies tools not in effectiveTools/allow) — this matches the pre-fix runtime, which already used the empty-profiles split; the fix deliberately made unified match split rather than enabling web-search."}
{"priorId":"XC-config-drift-3","status":"RESOLVED","area":"platform-config-xcut","evidence":"config/cron-jobs.example.json (git diff) added explicit `action` to all 5 rows (improvement/backup/memory_flush/cost_report/update_review), and apps/gateway/src/services/cron-job-config-helpers.ts:29-34 adds BUILT_IN_CRON_ACTIONS + line 133 `action: canonicalAction ?? sanitized.action ?? existing?.action ?? \"task\"` so built-in jobIds get the correct action even if a config copy still says task. Regression test cron-job-config-helpers.loop16.test.ts:160-216 proves action=task in config is repaired to improvement. No more misleading dead metadata / silent task fallback for the 5 shipped jobs."}
{"priorId":"XC-config-drift-4","status":"PARTIAL","area":"platform-config-xcut","evidence":"The config row self_improvement_weekly_replay now gets `action: improvement` (cron-jobs.example.json + BUILT_IN_CRON_ACTIONS in cron-job-config-helpers.ts:30), but the two-identity split is NOT reconciled: apps/gateway/src/services/gateway/cron-automation-service.ts:29 exports IMPROVEMENT_WEEKLY_JOB_ID=\"self_improvement_weekly_replay\" while apps/gateway/src/services/improvement-service.ts:476,510,514 still hardcodes \"improvement_weekly\" (never imports the constant — confirmed via grep). Startup gateway-service.ts:2157-2158 runs loadCronJobsFromConfig() then ensureWeeklyImprovementCronJob(), seeding BOTH rows with the identical display name. New failure mode introduced by this fix (see newFindings): because the config row now dispatches action=improvement -> runHandlers.improvement() (gateway-service.ts:1221) -> runWeeklyImprovementSchedulerIfDue() -> cronJobs.get(\"improvement_weekly\") (improvement-service.ts:476) `if (!job?.enabled) return`, enabling the config row silently no-ops. Pre-fix it dispatched a generic task (action defaulted to task). I wrote and ran a repro (real Storage + loadCronJobsFromConfig + ensureWeeklyImprovementCronJob + enable config row + runWeeklyImprovementSchedulerIfDue force) — it passes: two rows seeded, config row action=improvement, and listDecisionReplayRuns()===[] after enabling. The finding's stated harm shifted from 'generic task' to 'silent no-op', not eliminated."}
{"priorId":"XC-config-drift-5","status":"PARTIAL","area":"platform-config-xcut","evidence":"apps/gateway/src/config-sync-lib.ts had ZERO changes in range (git diff empty). syncUnifiedConfig still treats the unified file as source of truth and writes each section down into the split files every load (config-sync-lib.ts:90-104 `writeJsonIfChanged(outPath, normalized)`), with warnIfSplitIsNewer (line 99) still only warning. The generic edit-durability defect (a hand-edit to a split file is overwritten on next boot) is mechanically unchanged. Only the specific 'hand-added profiles to tool-policy.json' reproduction is defused because XC-config-drift-2 removed the profiles concept from the example. Plan explicitly declares drift-5 a duplicate root of drift-2; original severity Low."}
{"priorId":"XC-config-drift-6","status":"RESOLVED","area":"platform-config-xcut","evidence":"apps/gateway/src/config.ts (git diff 67c3adb64..a81eeccc4) flips withAssistantDefaults web.firecrawl to `enabled: firecrawlInput.enabled ?? true` (was false), `defaultReadBackend ?? \"firecrawl\"` (was native), and adds apiKeyEnv default \"FIRECRAWL_API_KEY\" — now matching the shipped assistant.config.example.json posture. config-defaults.test.ts:59-66 additionally asserts the example values, and .env.example documents GOATCITADEL_FIRECRAWL_ENABLED=true. Built-in default and example are no longer opposite."}
{"priorId":"XC-secrets-redaction-1 (platform-config duplicate)","status":"PARTIAL","area":"platform-config-xcut","evidence":"A new comprehensive redactor packages/contracts/src/secret-redaction.ts (redactSecretText: bearer/basic, env-assignment, api-key/token/secret/password/signature patterns, env-value scan) was added and wired into packages/contracts/src/channel-sanitizer.ts:185 (redactOutboundSecrets now delegates to it). BUT the runtime log sink packages/gateway-core/src/logger.ts had ZERO changes in range: SECRET_VALUE_PATTERNS at logger.ts:38-41 still matches only /sk-.../ and /Bearer .../, and the logger does not import redactSecretText. The finding's exact residual gap — tokens in string VALUES under benign keys or in error .message/.stack surfacing through the log sink — remains. The stronger redactor is still not on the primary log path."}
{"priorId":"XC-secrets-redaction-2 (platform-config duplicate)","status":"NOT_FIXED","area":"platform-config-xcut","evidence":"packages/policy-engine/src/sandbox/network-guard.ts had ZERO changes in range (git diff empty). The success-path fetch error is still re-thrown unmodified at network-guard.ts:491 `throw error;` with no redactUrlForError, exactly as the finding described (only the block/redirect paths redact). The surfacing points also unchanged: apps/gateway/src/services/integration-channel-activity.ts:109,147,229,469 still do `detail: error instanceof Error ? error.message : String(error)` with no redaction, and sendTelegramTypingIndicator (line 214-229) routes through the guarded fetch. A userinfo URL (https://bot:TOKEN@allowlisted-host/) throwing a TypeError still surfaces the credential-bearing URL raw in `detail`. The new redactSecretText was wired only into outbound channel-content sanitization, not the fetch-error path."}
{"priorId":"XC-perf-system-1","status":"NOT_FIXED","area":"platform-config-xcut","evidence":"apps/gateway/src/services/dashboard-route-service.ts had ZERO changes in range (git diff empty). getDashboardState still computes pendingApprovals via approvals.list(\"pending\", 10000).filter(...).length (dashboard-route-service.ts:227-229) — materialization-based, no COUNT. packages/storage/src/approval-repo.ts has no count/pendingCount method (only list() at line 185) and was not changed. The client poll apps/mission-control-next/src/app/use-shell-status.ts is unchanged: still refreshIntervalMs=15000 (line 47), paused only on doc.hidden (line 124), not SSE-gated. Both the server materialization and the ungated 15s poll persist. (Original reduced to Medium: 10k is a worst-case cap; latent under abnormal approval backlog.)"}
{"priorId":"XC-perf-system-2","status":"NOT_FIXED","area":"platform-config-xcut","evidence":"packages/threaded-surface-core/src/chat/chat-page-pure-helpers.ts had ZERO changes in range; resolveChatRefreshPlan still returns refreshSession:\"full\" for chat_thread_updated (chat-page-pure-helpers.ts:128-132), which drives loadSessionCoreState(3 fetches)+loadSessionSecondaryState(6 fetches)=9 round-trips per message-appended signal. The double-fetch is also intact: useChatOutboundExecution.ts:321-342 scheduleStreamMessageReconciliation still uses the 400ms timer, and its only in-range diff (line 951) is an unrelated 1-line composer-status tweak (queuedOutbound.length>0). The in-range chat changes address different findings: chat.shared.coalesce.test.ts (+53) is SSE-heartbeat (CHAT_SSE_HEARTBEAT_INTERVAL_MS); useChatSessionData.ts (+66) adds generation-guard/clearSessionScopedState for stale cross-session data (state-hooks), not fanout dedup. The 800ms SSE coalesce still does not dedupe the ~400ms-apart double thread fetch."}
{"priorId":"XC-windows-parity-1","status":"NOT_FIXED","area":"windows-regression-sweep","evidence":"apps/gateway/src/services/mcp-runtime.ts spawn path is UNTOUCHED across 67c3adb64..a81eeccc4 (git diff of this file shows zero changes to resolveSpawnCommand or withStdioMcpClient; the only diff to this file was the security commit 29ebf44a8 swapping sanitizeMcpRuntimeError to redactSecretText). withStdioMcpClient at :657-664 still calls spawn(resolveSpawnCommand(server.command??''), server.args??[], {stdio,cwd,env,windowsHide:true,timeout}) with NO shell:true and NO cmd.exe wrapper. resolveSpawnCommand at :1240-1242 still returns `${command}.cmd` on win32 for npx/npm/pnpm/yarn. On Node >=20.12/22 (CVE-2024-27980 fix) spawn('npx.cmd',args,{shell:false}) throws EINVAL SYNCHRONOUSLY, so child.on('error') at :725 never attaches and child.stdout at :690 never runs. The in-repo cmd.exe /d /s /c pattern the finding cited exists and was even applied to sibling scripts in this fix set (scripts/typescript/run-ts7-workspace.mjs:161, scripts/dev.mjs:147) but was NOT applied here. Test at mcp-runtime.test.ts:365 asserts resolveSpawnCommand('npx','win32')==='npx.cmd' (blesses the buggy output); spawn is mocked in mcp-runtime.lifecycle.test.ts:12 so the real EINVAL is never exercised. Verified suite still green: 24/24 pass. (See new finding R2-windows-regression-sweep-1.)"}
{"priorId":"XC-supply-chain-2","status":"NOT_FIXED","area":"windows-regression-sweep","evidence":"scripts/run-unless-env.mjs is UNTOUCHED in the fix range (not present in git diff --stat 67c3adb64..a81eeccc4). Lines 16-21 still spawn(resolveCommand(command), args, {cwd,env,stdio:'inherit',shell:false}) where resolveCommand returns 'pnpm.cmd' on win32 (:40-41); no try/catch wraps the spawn, so the synchronous EINVAL bypasses child.on('error') at :23. Plan claimed 'XC-windows-parity-1 covers XC-supply-chain-2' but parity-1 itself was not fixed, so this remains broken. Gateway pretest/presmoke hook still crashes on default Windows `pnpm --filter @goatcitadel/gateway test`. (See new finding R2-windows-regression-sweep-2.)"}
{"priorId":"XC-supply-chain-3","status":"NOT_FIXED","area":"windows-regression-sweep","evidence":"scripts/verify-extension-package-artifacts.mjs is UNTOUCHED in the fix range. Line 64 still calls spawnSync('tar', ['-xzf', tarball, '-C', extractDir], {encoding,shell:false}) with NO --force-local; tarball is an absolute os.tmpdir() path (C:\\Users\\...). GNU/MSYS2 tar treats 'C:' as an rsh host -> 'Cannot connect to C: resolve failed', exit 2. resolvePnpmPackArgs special-cases Windows for pnpm pack (:50) but the two tar spawnSync calls (:64 extract, and the -czf pack elsewhere) were never given --force-local. This is the verify:extensions:package gate + fast.extensions-sdk-package lane. (See new finding R2-windows-regression-sweep-3.)"}
{"priorId":"XC-supply-chain-4","status":"RESOLVED","area":"windows-regression-sweep","evidence":"docs/superpowers/specs/2026-07-01-mission-control-stream-design.md:5 changed in commit 2eefaa041: `linked to F:\\code\\personal-ai` -> `linked to this GoatCitadel checkout`. `node scripts/verify-repo-hygiene.mjs` now exits 0 ('Repo hygiene check passed.'). git grep confirms no remaining F:\\code / C:\\Users\\spurn in non-skipped tracked files (test/spec fixtures are in the new skip allowlist at verify-repo-hygiene.mjs:39-40). CI-unblocker (fast.repo-hygiene) is green; verify-repo-hygiene.test.mjs passes 6/6."}
{"priorId":"XC-windows-parity-5","status":"RESOLVED","area":"windows-regression-sweep","evidence":".gitattributes gained (commit 2eefaa041) `* text=auto eol=lf` plus `*.bat/*.cmd/*.ps1 text eol=crlf` (now :1-4). This normalizes the tracked install.sh LF shebang and prevents core.autocrlf=true from materializing bash\\r. Enforced going forward by a new collectGitAttributesEolPolicyFindings check in verify-repo-hygiene.mjs:127-139 (REQUIRED_GITATTRIBUTES_LINES) with tests at verify-repo-hygiene.test.mjs ('requires root EOL policy' / 'accepts complete root EOL policy')."}
{"priorId":"XC-supply-chain-1","status":"RESOLVED","area":"windows-regression-sweep","evidence":"apps/gateway/src/config.ts:978 flipped requireDigestPin default from `?? false` to `?? true` (commit 2eefaa041). apps/gateway/src/services/code-mode-execution-backends.ts:157 mirrors `requireDigestPin ?? true`, so digestPinBlocked=true for an enabled+tag-only image -> callable=false (fail-safe: backend silently unavailable rather than accepting a substituted image). Shipped assistant.config.example.json now explicitly ships codeModeDockerBackend.enabled:false + requireDigestPin:true (coherent). Tests updated: config-defaults.test.ts asserts requireDigestPin===true; config.test.ts 'defaults ... Docker digest posture to shipped fail-closed values'."}
{"priorId":"XC-supply-chain-5","status":"NOT_FIXED","area":"windows-regression-sweep","evidence":".github/workflows/security-trivy.yml is UNTOUCHED in the fix range and no `pnpm audit` step was added to any workflow (not in the 148-file diff). Trivy severity filter unchanged. This was an out-of-scope Low (latent CVE-floor gap); the plan's Platform/Config workstream listed it but no code change addresses it. Posture verified clean today so no live exposure."}
