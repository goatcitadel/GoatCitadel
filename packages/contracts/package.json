{
  "name": "@goatcitadel/contracts",
  "version": "1.0.0",
  "goatcitadelOwner": "contracts",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./citadel-vault-node": {
      "types": "./dist/citadel-vault-node.d.ts",
      "default": "./dist/citadel-vault-node.js"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/goatcitadel/GoatCitadel.git",
    "directory": "packages/contracts"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "zod": "^3.24.1"
  },
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc -b tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage.enabled --coverage.provider=v8 --coverage.reporter=json --coverage.reporter=text-summary --coverage.reportsDirectory=coverage",
    "lint": "tsc -b tsconfig.json"
  }
}
